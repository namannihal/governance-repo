{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CPF Module Schema: RedisCache and Database Service Pattern",
  "description": "JSON schema for CPF module 'cpf-azure-prdsvcpat-rediscache'. Use this to generate Terraform code that calls the CPF module.",
  "cpf_id": "cpf-azure-prdsvcpat-rediscache",
  "module_type": "service-pattern",
  "azure_service": "Rediscache",
  "module_name": "RedisCache and Database Service Pattern",
  "latest_tag": "",
  "available_tags": [],
  "gitlab_repository": "",
  "terraform_source": "git::https://gitlab.dx1.lseg.com/app/app-51310/azure/prdsvcpat/terraform/azure-prdsvcpat-terraform-rediscache.git//",
  "terraform_requirements": {
    "azurerm": ">= 3.51, <= 3.117",
    "time": "~>0.12.0",
    "terraform": ">= 1.5.0"
  },
  "prerequisites": [],
  "azure_resources_created": [
    {
      "terraform_resource": "time_rotating.expiration_date",
      "type": "resource"
    },
    {
      "terraform_resource": "time_sleep.wait_keyvault_pe",
      "type": "resource"
    },
    {
      "terraform_resource": "time_sleep.wait_redis_pe",
      "type": "resource"
    }
  ],
  "terraform_usage": {
    "module_call_template": {
      "module_label": "rediscache",
      "source": "git::https://gitlab.dx1.lseg.com/app/app-51310/azure/prdsvcpat/terraform/azure-prdsvcpat-terraform-rediscache.git//",
      "version": "latest",
      "available_versions": []
    }
  },
  "inputs": {
    "type": "object",
    "required": [
      "app_id",
      "capacity",
      "environment",
      "key_vault_config",
      "location",
      "org_id",
      "patch_schedule",
      "resource_group_name",
      "shared_nrtbl_vnet_id"
    ],
    "properties": {
      "app_id": {
        "type": "string",
        "description": "(Required) A descriptive name (abbreviation) for the workload (application) as it is registered in Leanix. The identifier format in Leanix is 'APP-12345', for the purpose of naming resources only 5 number will be used.",
        "required": true
      },
      "capacity": {
        "type": "number",
        "description": "(Required) The size of the Redis cache to deploy. Valid values for a `C` SKU family (`Basic` or `Standard`) are `0`, `1`, `2`, `3`, `4`, `5`, `6`. For `P` SKU family (`Premium`) valid values are `1`, `2`, `3`, `4`.",
        "required": true
      },
      "context": {
        "type": "string",
        "description": "(Optional) Application context information for the resource(s) (max 10 chars).",
        "required": false,
        "default": "null"
      },
      "deploy_as_secondary_linked_server": {
        "type": "bool",
        "description": "(Optional). Boolean, If set to true the Redis Cache is deployed and then linked as a secondary cache for geo-replication. Cannot be enabled when Zone-Redundancy is implemented.",
        "required": false,
        "default": "false"
      },
      "environment": {
        "type": "string",
        "description": "(Required) The environment where the resource is deployed. Given as abbreviation to leave more characters to the other naming components (max 3 chars).",
        "required": true
      },
      "firewall_private_ip_address": {
        "type": "string",
        "description": "(Optional). Azure firewall private Ip Address. Used when creating routes to firewall during route table creation.",
        "required": false,
        "default": "null"
      },
      "firewall_rules": {
        "type": "list(object({ name = string start_ip_address = string end_ip_address = string }))",
        "description": "\"(Optional) List of Azure Redis Cache firewall rule specification.\" object({ name = (Required) Specifies the name of the Firewall Rule. start_ip_address = (Required) The starting IP Address to allow through the firewall for this rule end_ip_address = (Required) The ending IP Address to allow through the firewall for this rule })",
        "required": false,
        "default": "[]"
      },
      "instance": {
        "type": "string",
        "description": "(Optional) Instance number, if context includes/requires multiple resources of the same type (max 3 int).",
        "required": false,
        "default": "null"
      },
      "key_vault_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) deploy_kv_and_pe = optional(bool, true) key_vault_id = optional(string, null) enabled_for_deployment = optional(bool, false) enabled_for_disk_encryption = optional(bool, true) enabled_for_template_deployment = optional(bool, false) soft_delete_retention_days = optional(number, 30) sku_name = optional(string, \"premium\") kv_secret_expiration_in_months = number kv_admin_role_app_spn_object_id = optional(string, null) kv_secret_instance = optional(string, null) secret_name_redis_primary_access_key = optional(string, null) secret_name_redis_secondary_access_key = optional(string, null) network_acls = optional(object({ bypass = optional(string, \"None\") default_action = optional(string, \"Deny\") ip_rules = optional(list(string), []) virtual_network_subnet_ids = optional(list(string), []) }), {}) private_endpoint = optional(object({ is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_id = optional(string, null) private_connection_resource_alias = optional(string, null) kv_ip_configuration = optional(map(object({ private_ip_address = string subresource_name = optional(string, \"vault\") member_name = optional(string, \"default\") })), {}) }), null) })",
        "description": "Object with cofigration for the Keyvault. As defined below context = context to be used for KeyVaults being deployed resources. instance = instance to be used for KeyVaults being deployed resources. deploy_kv_and_pe = (Optional). Boolean to deploy KeyVault and Private Endpoint for KeyVault. Default is true. key_vault_id = (Optional). KeyVault ID to be used for Private Endpoint for KeyVault. Required if deploy_kv_and_pe is false. enabled_for_deployment = (Optional). Boolean to enable KeyVault for deployment. Default is false. enabled_for_disk_encryption = (Optional). Boolean to enable KeyVault for disk encryption. Default is true. enabled_for_template_deployment = (Optional). Boolean to enable KeyVault for template deployment. Default is false. soft_delete_retention_days = (Optional) The number of days that items should be retained for once soft-deleted. sku_name = (Optional). Sku name for KeyVault. Default is premium. kv_secret_expiration_in_months = (Required). Number of months for which the secrets in KeyVault should be valid. kv_admin_role_app_spn_object_id = (Required). Object ID for the SPN to add KeyVault administrator role. kv_secret_instance = (Optional). Instance to be used for KeyVault secrets being deployed resources. secret_name_redis_primary_access_key = (Optional). Secret name for Redis primary access key. secret_name_redis_secondary_access_key = (Optional). Secret name for Redis secondary access key. network_acls = (Optional). Object with network ACLs for KeyVault. As defined below { bypass = (Optional). Specifies whether traffic is bypassed for Azure services. Default is None. Possible values are None, AzureServices, Logging, Metrics, or AzureServices,Logging,Metrics. default_action = (Optional). Specifies the default action of allow or deny when no other rules match. Default is Deny. Possible values are Allow and Deny. ip_rules = (Optional). List of IP addresses or CIDR ranges to whitelist. Only IPV4 addresses are allowed. This list must not include IP address ranges within Azure services. This list can include IP addresses ranges from on-premises ranges. virtual_network_subnet_ids = (Optional). List of virtual network subnet ids to whitelist. This list must not include IP address ranges within Azure services. } private_endpoint = (Optional). Object with private endpoint configuration for KeyVault. As defined below { is_manual_connection = (Optional). Boolean to indicate if the connection is manual. Default is false. static_ip_required = (Optional). Boolean to indicate if static IP is required. Default is false. private_connection_resource_id = (Optional). Resource ID of the private connection. Required if is_manual_connection is true. private_connection_resource_alias = (Optional). Resource alias of the private connection. Required if is_manual_connection is true. kv_ip_configuration = (Optional). Object with IP configuration for KeyVault. As defined below { private_ip_address = (Required). Private IP address of the IP configuration. subresource_name = (Optional). Subresource name of the IP configuration. Default is vault. member_name = (Optional). Member name of the IP configuration. Default is default. } }",
        "required": true
      },
      "key_vault_tags": {
        "type": "map(any)",
        "description": "(Optional) Key Vault related tags to be set on key vault child resources.",
        "required": false,
        "default": "{}"
      },
      "location": {
        "type": "string",
        "description": "(Required) Location of the resource group.",
        "required": true
      },
      "network_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) use_vnet_injection = optional(bool, false) nsg_security_rules = optional(map(object({ name = string description = string priority = number direction = string access = string protocol = string source_port_range = string destination_port_range = string source_address_prefix = string destination_address_prefix = string source_application_security_group_ids = list(string) destination_application_security_group_ids = list(string) })), {}) route_table = optional(object({ route_table_id = optional(string, null) route = optional(map(object({ name = string address_prefix = string next_hop_type = string next_hop_in_ip_address = string })), null) disable_bgp_route_propagation = optional(bool, false) }), { route_table_id = null disable_bgp_route_propagation = false route = null }) redis_subnet = optional(object({ subnet_id = optional(string, null) address_prefix = optional(string, null) private_static_ip_address = optional(string, null) enforce_private_link_endpoint_network_policies = optional(string, \"Disabled\") private_link_service_network_policies_enabled = optional(bool, false) }), null) private_endpoint = optional(object({ is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_id = optional(string, null) private_connection_resource_alias = optional(string, null) ip_configuration = optional(map(object({ private_ip_address = string subresource_name = optional(string, \"redisCache\") member_name = optional(string, \"redisCache\") })), {}) }), null) })",
        "description": "Object with cofigration for Network, As Defined below context = context to be used for all networking resources. instance = instance tpo be used for all networking resources. use_vnet_injection = (Optional) Whether to use vnet Injection with Redis cache, Requires Premium SKU, Defaults to false. redis_subnet = (Optional). Only required when use_vnet_injection is set to true => { address_prefix = IP address CIDRs for deploying the subnet where Redis is going to be Deployed. if use_vnet_injection is set to true. private_static_ip_address = (Optional). The Static IP Address to assign to the Redis Cache when hosted inside the Virtual Network. Changing this forces a new resource to be created. enforce_private_link_endpoint_network_policies = (Optional) Enable or disable network policies for the Private Endpoint on the subnet. private_link_service_network_policies_enabled = (Optional) Enable or disable network policies for the Private Link Service on the subnet. } nsg_security_rules = (Optional). Map of objects mentioned below security rules of the NSG to be deployed and attached with PSQL Subnet. As defined below { name = \"(Required) The name of the security rule.\" description = \"(Optional) A description for this rule. Restricted to 140 characters.\" priority = \"(Required) Specifies the priority of the rule. The value can be between 100 and 4096. The priority number must be unique for each rule in the collection. The lower the priority number, the higher the priority of the rule.\" direction = \"(Required) The direction specifies if rule will be evaluated on incoming or outgoing traffic. Possible values are Inbound and Outbound.\" access = \"(Required) Specifies whether network traffic is allowed or denied. Possible values are Allow and Deny.\" protocol = \"(Required) Network protocol this rule applies to. Possible values include Tcp, Udp, Icmp, Esp, Ah or * (which matches all).\" source_port_range = \"(Optional) Source Port or Range. Integer or range between 0 and 65535 or * to match any. This is required if source_port_ranges is not specified.\" destination_port_range = \"(Optional) Destination Port or Range. Integer or range between 0 and 65535 or * to match any. This is required if destination_port_ranges is not specified.\" source_address_prefix = \"(Optional) CIDR or source IP range or * to match any IP. Tags such as VirtualNetwork, AzureLoadBalancer and Internet can also be used. This is required if source_address_prefixes is not specified.\" destination_address_prefix = \"(Optional) CIDR or destination IP range or * to match any IP. Tags such as VirtualNetwork, AzureLoadBalancer and Internet can also be used. This is required if destination_address_prefixes is not specified.\" source_application_security_group_ids = \"(Optional) A List of source Application Security Group IDs\" destination_application_security_group_ids = \"(Optional) A List of destination Application Security Group IDs\" } route_table = (Optional). Object with route table configuration for PSQL Subnet. As defined below { route_table_id = (Optional). Route table ID to be used for PSQL Subnet. Required if route is not provided. route = (Optional). A Map of objects mentioned below for route configuration of the route table. { name = \"(Required) The name of the route.\" address_prefix = \"(Required) The destination CIDR to which the route applies.\" next_hop_type = \"(Required) The type of Azure hop the packet should be sent to. Possible values are VirtualNetworkGateway, VnetLocal, Internet, VirtualAppliance and None.\" next_hop_in_ip_address = \"(Optional) The IP address packets should be forwarded to. Next hop values are only allowed in routes where the next hop type is VirtualAppliance.\" } disable_bgp_route_propagation = (Optional). Boolean flag which controls propagation of routes learned by BGP on that route table. True means disable. False means enable. Default is false. }",
        "required": false,
        "default": "null"
      },
      "org_id": {
        "type": "string",
        "description": "(Required) Three letter code representing organization/tenant/CSP.",
        "required": true
      },
      "patch_schedule": {
        "type": "object({ day_of_week = string start_hour_utc = optional(number, 5) })",
        "description": "\"(Optional) The scheduled maintenance for Azure Cache for Redis service to regularly updates the cache with the latest platform features and fixes.\" object({ day_of_week = (Required) the Weekday name - possible values include Monday, Tuesday, Wednesday etc. start_hour_utc = (Optional) the Start Hour for maintenance in UTC - possible values range from 0 - 23. })",
        "required": true
      },
      "policy_assignments": {
        "type": "map(object({ name = string access_policy_name = string object_id = string object_id_alias = string }))",
        "description": "\"(Optional) Map of Access policy assignments for redis cache.\" object({ name = (Required) The name of the Redis Cache Access Policy Assignment. Changing this forces a new Redis Cache Access Policy Assignment to be created. access_policy_name = (Required) The name of the Access Policy to be assigned. Changing this forces a new Redis Cache Access Policy Assignment to be created. object_id = (Required) The principal ID to be assigned the Access Policy. Changing this forces a new Redis Cache Access Policy Assignment to be created. object_id_alias = (Required) The alias of the principal ID. User-friendly name for object ID. Also represents username for token based authentication. Changing this forces a new Redis Cache Access Policy Assignment to be created. })",
        "required": false,
        "default": "{}"
      },
      "primary_redis_cache_name": {
        "type": "string",
        "description": "(Optional). The name of the primary Redis Cache. Required when 'deploy_as_secondary_linked_server' is set to true the Redis Cache is deployed and then linked as a secondary cache for geo-replication.",
        "required": false,
        "default": "null"
      },
      "primary_redis_cache_resource_group_name": {
        "type": "string",
        "description": "(Optional). The name of the resource group where primary Redis Cache is deployed. Required when 'deploy_as_secondary_linked_server' is set to true the Redis Cache is deployed and then linked as a secondary cache for geo-replication.",
        "required": false,
        "default": "null"
      },
      "privateendpoint_subnet_id": {
        "type": "string",
        "description": "(Required.) The resource id of the subnet to deploy private endpoints into.",
        "required": false,
        "default": "null"
      },
      "redis_configuration": {
        "type": "object({ aof_backup_enabled = optional(bool, false) aof_storage_connection_string_0 = optional(string, null) aof_storage_connection_string_1 = optional(string, null) enable_authentication = optional(bool, true) active_directory_authentication_enabled = optional(bool, false) maxmemory_reserved = optional(number, 10) maxmemory_delta = optional(number, 2) maxfragmentationmemory_reserved = optional(number, 10) maxmemory_policy = optional(string, null) rdb_backup_enabled = optional(bool, false) rdb_backup_frequency = optional(number) rdb_backup_max_snapshot_count = optional(number, null) rdb_storage_connection_string = optional(number, null) })",
        "description": "\"(Optional) Amount of replicas to create per master for this Redis Cache.\" object({ aof_backup_enabled = (Optional) Enable or disable AOF persistence for this Redis Cache. Defaults to false. aof_storage_connection_string_0 = (Optional) First Storage Account connection string for AOF persistence. aof_storage_connection_string_1 = (Optional) Second Storage Account connection string for AOF persistence. enable_authentication = (Optional) If set to false, the Redis instance will be accessible without authentication. Defaults to true. active_directory_authentication_enabled = (Optional) Enable Microsoft Entra (AAD) authentication. Defaults to false. maxmemory_reserved = (Optional) Value in megabytes reserved for non-cache usage e.g. failover. Defaults are shown below. maxmemory_delta = (Optional) The max-memory delta for this Redis instance. Defaults are shown below. maxfragmentationmemory_reserved = (Optional) Value in megabytes reserved to accommodate for memory fragmentation. Defaults are shown below. maxmemory_policy = (Optional) How Redis will select what to remove when maxmemory is reached. Defaults are shown below. Defaults to volatile-lru. rdb_backup_enabled = (Optional) Is Backup Enabled? Only supported on Premium SKUs. Defaults to false. rdb_backup_frequency = (Optional) The Backup Frequency in Minutes. Only supported on Premium SKUs. Possible values are: 15, 30, 60, 360, 720 and 1440. rdb_backup_max_snapshot_count = (Optional) The maximum number of snapshots to create as a backup. Only supported for Premium SKUs. rdb_storage_connection_string = (Optional) The Connection String to the Storage Account. Only supported for Premium SKUs. })",
        "required": false,
        "default": "{ \"active_directory_authentication_enabled\": false, \"aof_backup_enabled\": false, \"aof_storage_connection_string_0\": null, \"aof_storage_connection_string_1\": null, \"enable_authentication\": true, \"maxfragmentationmemory_reserved\": 10, \"maxmemory_delta\": 2, \"maxmemory_policy\": null, \"maxmemory_reserved\": 10, \"rdb_backup_enabled\": false, \"rdb_backup_frequency\": null, \"rdb_backup_max_snapshot_count\": null, \"rdb_storage_connection_string\": null }"
      },
      "replicas_per_master": {
        "type": "number",
        "description": "(Optional) Amount of replicas to create per master for this Redis Cache.",
        "required": false,
        "default": "null"
      },
      "replicas_per_primary": {
        "type": "number",
        "description": "(Optional) Amount of replicas to create per primary for this Redis Cache. If both replicas_per_primary and replicas_per_master are set, they need to be equal.",
        "required": false,
        "default": "null"
      },
      "resource_group_name": {
        "type": "string",
        "description": "(Reqired). The name of the Application Resource Group. This Resource group usually contains the Application resources(app infra resources).",
        "required": true
      },
      "shard_count": {
        "type": "number",
        "description": "(Optional) Only available when using the Premium SKU The number of Shards to create on the Redis Cluster.",
        "required": false,
        "default": "null"
      },
      "shared_nrtbl_vnet_id": {
        "type": "string",
        "description": "(Reqired). The ARM Resource Id of the Non-Routeable Virtual Network in shared Resource group.",
        "required": true
      },
      "sku": {
        "type": "string",
        "description": "(Required) The SKU of Redis Cache. Possible values are `Basic`, `Standard` or `Premium`.",
        "required": false,
        "default": "\"Standard\""
      },
      "tags": {
        "type": "map(any)",
        "description": "(Optional) Tags to be set on each resource.",
        "required": false,
        "default": "{}"
      },
      "use_system_assigned_identity": {
        "type": "bool",
        "description": "(Required) Indicates that the system assigned identity should be used for CMK or not. Defaults to `false`.",
        "required": false,
        "default": "false"
      },
      "zones": {
        "type": "list(string)",
        "description": "(Optional) Specifies a list of Availability Zones in which this Redis Cache should be located. Changing this forces a new Redis Cache to be created.",
        "required": false,
        "default": "[]"
      }
    }
  },
  "outputs": {
    "keyvault": {
      "description": "The keyvault module outputs."
    },
    "keyvault-pe": {
      "description": "The keyvault-pe module outputs."
    },
    "keyvaultsecret_redis-primary_access_key": {
      "description": "The KeyVault Secret Module outputs for primary access key."
    },
    "keyvaultsecret_redis-secondary_access_key": {
      "description": "The KeyVault Secret Module outputs for secondary access key."
    },
    "networksecuritygroup": {
      "description": "The networksecuritygroup module outputs."
    },
    "privateendpoint-redis": {
      "description": "The privateendpoint redis module outputs."
    },
    "rediscache": {
      "description": "The rediscache module outputs."
    },
    "redislinkedserver": {
      "description": "The redislinkedserver redis module outputs."
    },
    "routetable": {
      "description": "The routetable module outputs."
    },
    "subnet": {
      "description": "The subnet module outputs."
    },
    "userassignedidentity": {
      "description": "The userassignedidentity module outputs."
    }
  },
  "agent_instructions": {
    "summary": "To provision 'Rediscache' via Terraform, call this CPF module with the inputs defined below. Required inputs have no default and must be provided.",
    "module_type_notes": "This is a service pattern module. It bundles multiple related services/configurations.",
    "naming_convention": "Resource names are auto-generated by the module using app_id, environment, and region.",
    "prerequisite_modules": []
  }
}