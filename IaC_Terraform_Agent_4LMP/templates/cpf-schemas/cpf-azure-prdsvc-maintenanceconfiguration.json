{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CPF Module Schema: __product name__ module",
  "description": "JSON schema for CPF module 'cpf-azure-prdsvc-maintenanceconfiguration'. Use this to generate Terraform code that calls the CPF module.",
  "cpf_id": "cpf-azure-prdsvc-maintenanceconfiguration",
  "module_type": "service",
  "azure_service": "Maintenanceconfiguration",
  "module_name": "__product name__ module",
  "latest_tag": "1.0.1",
  "available_tags": [
    "1.0.1",
    "1.0.0",
    "0.1.1",
    "0.1.0"
  ],
  "gitlab_repository": "",
  "terraform_source": "git::https://gitlab.dx1.lseg.com/app/app-51310/azure/prdsvc/terraform/azure-prdsvc-terraform-maintenanceconfiguration.git//?ref=1.0.1",
  "terraform_requirements": {
    "azurerm": ">=4.33",
    "terraform": ">= 1.5.0"
  },
  "prerequisites": [],
  "azure_resources_created": [
    {
      "terraform_resource": "azurerm_maintenance_assignment_dynamic_scope.this",
      "type": "resource"
    },
    {
      "terraform_resource": "azurerm_maintenance_configuration.this",
      "type": "resource"
    },
    {
      "terraform_resource": "azurerm_resource_group.this",
      "type": "data source"
    }
  ],
  "terraform_usage": {
    "module_call_template": {
      "module_label": "maintenanceconfiguration",
      "source": "git::https://gitlab.dx1.lseg.com/app/app-51310/azure/prdsvc/terraform/azure-prdsvc-terraform-maintenanceconfiguration.git//?ref=1.0.1",
      "version": "1.0.1",
      "available_versions": [
        "1.0.1",
        "1.0.0",
        "0.1.1",
        "0.1.0"
      ]
    }
  },
  "inputs": {
    "type": "object",
    "required": [
      "app_id",
      "environment",
      "location",
      "org_id",
      "resource_group_name",
      "scope"
    ],
    "properties": {
      "app_id": {
        "type": "string",
        "description": "(Required) A descriptive name (abbreviation) for the workload (application) as it is registered in Leanix. The identifier format in Leanix is 'APP-12345', for the purpose of naming resources only 5 number will be used.",
        "required": true
      },
      "context": {
        "type": "string",
        "description": "(Optional) Application context information for the resource(s) (max 10 chars).",
        "required": false,
        "default": "null"
      },
      "context_suffix": {
        "type": "string",
        "description": "(Optional) Application context suffix information for the resource(s) (max 3 chars).",
        "required": false,
        "default": "null"
      },
      "dynamic_scope": {
        "type": "object({ name = string filter = object({ locations = optional(list(string)) os_types = optional(list(string)) resource_groups = optional(list(string)) resource_types = optional(list(string)) tag_filter = optional(string) tags = optional(list(object({ tag = string values = list(string) }))) }) })",
        "description": "dynamic_scope = \"(Optional) A dynamic_scope object block variable to define the dynamic scope of the Maintenance Assignment.Only valid for InGuestPatch Maintenance Configuration Scopes.\" dynamic_scope = (Optional) object({ name = \"(Required) The name which should be used for this Dynamic Maintenance Assignment. Changing this forces a new Dynamic Maintenance Assignment to be created. The name must be unique per subscription.\" filter = \"(Required) A filter object block to define the filter values for the dynamic scope as defined below.\" locations = \"(Optional) Specify a list of allowed locations for the VMs to be patched.\" os_types = \"(Optional) Specify a list of allowed OS types for the VMs to be patched.\" resource_groups = \"(Optional) Specify a list of allowed resource groups for the VMs to be patched.\" resource_types = \"(Optional) Specify a list of allowed resource types for the VMs to be patched.\" tag_filter = \"(Optional) Filter VMs by Any or All specified tags. Defaults to Any.\" tags = \"(Optional) A list object block to define the tags to filter by as defined below.\" tag = \"(Required) Specify the tag to filter by.\" values = \"(Required) Specify a list of values the defined tag can have.\" })",
        "required": false,
        "default": "null"
      },
      "environment": {
        "type": "string",
        "description": "(Required) The environment where the resource is deployed. Given as abbreviation to leave more characters to the other naming components (max 3 chars).",
        "required": true
      },
      "in_guest_user_patch_mode": {
        "type": "string",
        "description": "(Optional) The in guest user patch mode. Possible values are Platform or User. Must be specified when scope is InGuestPatch",
        "required": false,
        "default": "null"
      },
      "install_patches": {
        "type": "object({ linux = optional(object({ classifications_to_include = optional(list(string)) package_names_mask_to_exclude = optional(list(string)) package_names_mask_to_include = optional(list(string)) })) windows = optional(object({ classifications_to_include = optional(list(string)) kb_numbers_to_exclude = optional(list(string)) kb_numbers_to_include = optional(list(string)) })) reboot = optional(string) })",
        "description": "Install_patches = \"(Optional) An install_patches object block variable to define the patching configuration, it must be specified when scope variable is set to InGuestPatch. Supports the following attributes:\" install_patches = (Optional) object({ linux = \"(Optional) A linux object block to define the patching configuration for Linux VMs as defined below.\" classifications_to_include = \"(Optional) Specify the list of Classification category of patches to be patched. Possible values are Critical, Security and Other.\" package_names_mask_to_exclude = \"(Optional) Specify the list of package names to be excluded from patching.\" package_names_mask_to_include = \"(Optional) Specify the list of package names to be included for patching.\" windows = \"(Optional) A windows object block to define the patching configuration for Windows VMs as defined below.\" classifications_to_include = \"(Optional) Specify the list of Classification category of patches to be patched. Possible values are Critical, Security, UpdateRollup, FeaturePack, ServicePack, Definition, Tools and Updates.\" kb_numbers_to_exclude = \"(Optional) Specify the list of KB numbers to be excluded from patching.\" kb_numbers_to_include = \"(Optional) Specify the list of KB numbers to be included for patching.\" reboot = \"(Optional) Specify the possible reboot preference, based on which it would be decided to reboot the machine or not after the patch operation is completed. Possible values are Always, IfRequired and Never.\" })",
        "required": false,
        "default": "null"
      },
      "instance": {
        "type": "string",
        "description": "(Optional) Instance number, if context includes/requires multiple resources of the same type (max 3 int).",
        "required": false,
        "default": "null"
      },
      "location": {
        "type": "string",
        "description": "(Required) Location of the resource group.",
        "required": true
      },
      "org_id": {
        "type": "string",
        "description": "(Required) Three letter code representing organization/tenant/CSP.",
        "required": true
      },
      "properties": {
        "type": "map(any)",
        "description": "(Optional) A mapping of properties to assign to the resource.",
        "required": false,
        "default": "{}"
      },
      "resource_group_name": {
        "type": "string",
        "description": "(Required) Name of the Resource Group in which to create the resource.",
        "required": true
      },
      "scope": {
        "type": "string",
        "description": "(Required) The scope of the Maintenance Configuration. Possible values are Extension, Host, InGuestPatch, OSImage, SQLDB or SQLManagedInstance.",
        "required": true
      },
      "tags": {
        "type": "map(any)",
        "description": "(Optional) Tags to be set on each resource.",
        "required": false,
        "default": "{}"
      },
      "visibility": {
        "type": "string",
        "description": "(Optional) The visibility of the Maintenance Configuration. The only allowable value is Custom.",
        "required": false,
        "default": "\"Custom\""
      },
      "window": {
        "type": "object({ start_date_time = string expiration_date_time = optional(string) duration = optional(string) time_zone = string recur_every = optional(string) })",
        "description": "window = (Optional) A window object block variable to define the maintenance window schedule. It supports the following attributes: window = (Optional) object({ start_date_time = \"(Required) The effective start date of the maintenance window in YYYY-MM-DD hh:mm format.\" expiration_date_time = \"(Optional) The effective expiration date of the maintenance window in YYYY-MM-DD hh:mm format.\" duration = \"(Optional) The duration of the maintenance window in HH:mm format.\" time_zone = \"(Required) The time zone for the maintenance window. A list of timezones can be obtained by executing [System.TimeZoneInfo]::GetSystemTimeZones() in PowerShell.\" recur_every = \"(Optional) The rate at which a maintenance window is expected to recur. The rate can be expressed as daily, weekly, or monthly schedules.\" })",
        "required": false,
        "default": "null"
      }
    }
  },
  "outputs": {
    "id": {
      "description": "The Resource ID of the Maintenance Configuration."
    },
    "name": {
      "description": "The Name of the Maintenance Configuration."
    },
    "resource": {
      "description": "The Maintenance Configuration resource."
    }
  },
  "agent_instructions": {
    "summary": "To provision 'Maintenanceconfiguration' via Terraform, call this CPF module with the inputs defined below. Required inputs have no default and must be provided.",
    "module_type_notes": "This is a single-service CPF module. It provisions one Azure resource type.",
    "naming_convention": "Resource names are auto-generated by the module using app_id, environment, and region.",
    "prerequisite_modules": []
  }
}