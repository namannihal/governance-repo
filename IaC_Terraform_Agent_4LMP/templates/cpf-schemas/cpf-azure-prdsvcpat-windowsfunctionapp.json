{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CPF Module Schema: Azure Functions Windows Service Pattern",
  "description": "JSON schema for CPF module 'cpf-azure-prdsvcpat-windowsfunctionapp'. Use this to generate Terraform code that calls the CPF module.",
  "cpf_id": "cpf-azure-prdsvcpat-windowsfunctionapp",
  "module_type": "service-pattern",
  "azure_service": "Windowsfunctionapp",
  "module_name": "Azure Functions Windows Service Pattern",
  "latest_tag": "",
  "available_tags": [],
  "gitlab_repository": "",
  "terraform_source": "git::https://gitlab.dx1.lseg.com/app/app-51310/azure/prdsvcpat/terraform/azure-prdsvcpat-terraform-windowsfunctionapp.git//",
  "terraform_requirements": {
    "random": "3.6.0",
    "azapi": ">= 1.9",
    "terraform": ">= 1.5.0",
    "azurerm": ">= 4.33, < 5.0",
    "azuread": "~> 2.47.0",
    "time": "~>0.11.1"
  },
  "prerequisites": [],
  "azure_resources_created": [
    {
      "terraform_resource": "time_sleep.wait_Windowsfunctionapp_pe",
      "type": "resource"
    },
    {
      "terraform_resource": "time_sleep.wait_keyvault_pe",
      "type": "resource"
    },
    {
      "terraform_resource": "time_sleep.wait_storage_pe",
      "type": "resource"
    }
  ],
  "terraform_usage": {
    "module_call_template": {
      "module_label": "windowsfunctionapp",
      "source": "git::https://gitlab.dx1.lseg.com/app/app-51310/azure/prdsvcpat/terraform/azure-prdsvcpat-terraform-windowsfunctionapp.git//",
      "version": "latest",
      "available_versions": []
    }
  },
  "inputs": {
    "type": "object",
    "required": [
      "app_id",
      "environment",
      "key_vault_config",
      "location",
      "org_id",
      "resource_group_name",
      "shared_nrtbl_vnet_id"
    ],
    "properties": {
      "app_id": {
        "type": "string",
        "description": "(Required) A descriptive name (abbreviation) for the workload (application) as it is registered in Leanix. The identifier format in Leanix is 'APP-12345', for the purpose of naming resources only 5 number will be used.",
        "required": true
      },
      "app_service_active_slot": {
        "type": "object({ slot_key = string overwrite_network_config = optional(bool, false) })",
        "description": "(Optional) Configuration for the active slot swap. slot_key = \"(Required) The key of the slot to swap. Must match a key in deployment_slots map.\" overwrite_network_config = \"(Optional) Should the network configuration be overwritten when swapping slots. Defaults to false.\"",
        "required": false,
        "default": "null"
      },
      "app_settings": {
        "type": "map(any)",
        "description": "(Optional) Function App application settings",
        "required": false,
        "default": "{}"
      },
      "applicationinsights_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) deploy_appinsights = optional(bool, true) appinsights_id = optional(string, null) connection_string = optional(string, null) instrumentation_key = optional(string, null) deploy_uai_metrics_publisher = optional(bool, true) uai_principal_id = optional(string, null) application_type = optional(string, null) daily_data_cap_in_gb = optional(number, 100) daily_data_cap_notifications_disabled = optional(bool, true) retention_in_days = optional(number, 90) sampling_percentage = optional(number, 100) disable_ip_masking = optional(bool, false) internet_ingestion_enabled = optional(bool, true) internet_query_enabled = optional(bool, true) force_customer_storage_for_profiler = optional(bool, false) api_key_enabled = optional(bool, true) read_permissions = optional(list(string), [\"agentconfig\", \"aggregate\", \"api\", \"draft\", \"extendqueries\", \"search\"]) write_permissions = optional(list(string), [\"annotations\"]) local_authentication_disabled = optional(bool, true) tags = optional(map(any), {}) })",
        "description": "Object with cofigration for Log anlytics Workspace , As Defined below context = \"(Optional) The context to be used for App Insights. If not provided the pattern conterxt is picked.\" instance = \"(Optional) The instance to be used for App Insights. If not provided the pattern conterxt is picked.\" deploy_appinsights = \"(Optional) Whether to deploy App Insights. Defaults to True.\" appinsights_id = \"(Optional) The ARM resource id for App Insights.\" connection_string = optional(string, null) instrumentation_key = optional(string, null) deploy_uai_metrics_publisher = \"(Optional) Whether to deploy User Assigned Identity which should be used be assigned the Monitoring Metrics Publisher role. Defaults to True.\" uai_principal_id = \"(Optional) Principal id of the User Assigned Identity which should be used be assigned the Monitoring Metrics Publisher role\" application_type = \"(Required) Specifies the type of Application Insights to create. Changing this forces a new resource to be created\" daily_data_cap_in_gb = \"(Optional) Specifies the Application Insights component daily data volume cap in GB. Default is 100.\" daily_data_cap_notifications_disabled = \"(Optional) Specifies if a notification email will be send when the daily data volume cap is met. Defaults to true.\" retention_in_days = \"(Optional) Specifies the retention period in days.\" sampling_percentage = \"(Optional) Specifies the percentage of the data produced by the monitored application that is sampled for Application Insights telemetry. Default is 100.\" disable_ip_masking = \"(Optional) Disables masking and log the real client IP.\" internet_ingestion_enabled = \"(Optional) Should the Application Insights component support ingestion over the Public Internet? Default is true.\" internet_query_enabled = \"(Optional) Should the Application Insights component support querying over the Public Internet? Default is true\" force_customer_storage_for_profiler = \"(Optional) Should the Application Insights component force users to create their own storage account for profiling? Deafult is false.\" api_key_enabled = \"(Optional) Should api key created or skip as per input. Default is true\" read_permissions = \"(Optional) Specifies the list of read permissions granted to the API key. Valid values are agentconfig, aggregate, api, draft, extendqueries, search. Please note these values are case sensitive. Changing this forces a new resource to be created.\" write_permissions = \"(Optional) Specifies the list of write permissions granted to the API key. Valid values are annotations. Please note these values are case sensitive. Changing this forces a new resource to be created.\" local_authentication_disabled = \"(Optional) Should local authentication be disabled for this Application Insights component? Default is true.\" tags = \"(Optional) A mapping of tags to assign to the Application Insights component. Default is {}.\"",
        "required": false,
        "default": "null"
      },
      "appserviceplan_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) deploy_appserviceplan = optional(bool, true) appserviceplan_id = optional(string, null) required_for_ase = optional(bool, null) sku_name = optional(string, null) os_type = optional(string, null) app_service_environment_id = optional(string, null) ase_sku_name = optional(string, \"I1v2\") worker_count = optional(number, 2) maximum_elastic_worker_count = optional(number, 2) per_site_scaling_enabled = optional(bool, false) zone_balancing_enabled = optional(bool, false) })",
        "description": "Object with cofigration for App Service Plan , As Defined below context = \"(Optional) The context to be used for App Insights. If not provided the pattern conterxt is picked.\" instance = \"(Optional) The instance to be used for App Insights. If not provided the pattern conterxt is picked.\" deploy_appserviceplan = \"(Optional) Whether to deploy App Insights. Defaults to True.\" appserviceplan_id = \"(Optional) The ARM resource id for App Insights.\" required_for_ase = \"(Required) ASP required for App Service Environment V3?\" sku_name = \"(Required) The SKU for the plan. Possible values include B1, B2, B3, D1, F1, I1, I2, I3, I1v2, I2v2, I3v2, I4v2, I5v2, I6v2, P1v2, P2v2, P3v2, P0v3, P1v3, P2v3, P3v3, P1mv3, P2mv3, P3mv3, P4mv3, P5mv3, S1, S2, S3, SHARED, EP1, EP2, EP3, WS1, WS2, WS3, and Y1.\" os_type = \"(Required) The O/S type for the App Services to be hosted in this plan. Possible values include Windows, Linux, and WindowsContainer. Changing this forces a new resource to be created.\" app_service_environment_id = \"(Optional) The ID of the App Service Environment to create this Service Plan in.\" ase_sku_name = \"(Optional) The SKU for the plan for App Service Environments. Possible values include I1v2, I2v2, I3v2, I4v2, I5v2, I6v2.\" worker_count = \"(Optional) The number of Workers (instances) to be allocated. Defaults to 2.\" maximum_elastic_worker_count = \"(Optional) The maximum number of workers to use in an Elastic SKU Plan. Cannot be set unless using an Elastic SKU. Defaults to 2.\" per_site_scaling_enabled = \"(Optional) Should Per Site Scaling be enabled.\" zone_balancing_enabled = \"(Optional) Should the Service Plan balance across Availability Zones in the region.\"",
        "required": false,
        "default": "null"
      },
      "auth_settings": {
        "type": "object({ enabled = bool additional_login_parameters = optional(map(string)) allowed_external_redirect_urls = optional(list(string)) default_provider = optional(string) issuer = optional(string) runtime_version = optional(string) token_refresh_extension_hours = optional(string, 72) token_store_enabled = optional(bool, false) unauthenticated_client_action = optional(string) active_directory = optional(object({ client_id = string allowed_audience = optional(list(string)) client_secret = optional(string) client_secret_setting_name = optional(string) }), null) })",
        "description": "(Optional) The `auth_settings` block supports the following: enabled = \"(Required) Should the Authentication / Authorization feature be enabled for the Windows Function App?\" additional_login_parameters = \"(Optional) Specifies a map of login Parameters to send to the OpenID Connect authorization endpoint when a user logs in.\" allowed_external_redirect_urls = \"(Optional) Specifies a list of External URLs that can be redirected to as part of logging in or logging out of the Windows Function App.\" default_provider = \"(Optional) The default authentication provider to use when multiple providers are configured. Possible values include: `AzureActiveDirectory`, `Facebook`, `Google`, `MicrosoftAccount`, `Twitter`, `Github`.\" issuer = \"(Optional) The OpenID Connect Issuer URI that represents the entity which issues access tokens for this Windows Function App.\" runtime_version = \"(Optional) The Runtime Version of the Authentication / Authorization feature in use for the Windows Function App.\" token_refresh_extension_hours = \"(Optional) The number of hours after session token expiration that a session token can be used to call the token refresh API. Defaults to 72 hours.\" token_store_enabled = \"(Optional) Should the Windows Function App durably store platform-specific security tokens that are obtained during login flows? Defaults to `false.`\" unauthenticated_client_action = \"(Optional) The action to take when an unauthenticated client attempts to access the app. Possible values include: `RedirectToLoginPage`, `AllowAnonymous`.\" active_directory = (Optional) object({ client_id = \"(Required) The ID of the Client to use to authenticate with Azure Active Directory.\" allowed_audiences = \"(Optional) Specifies a list of Allowed audience values to consider when validating JWTs issued by Azure Active Directory.\" client_secret = \"(Optional) The Client Secret for the Client ID. Cannot be used with client_secret_setting_name.\" client_secret_setting_name = \"(Optional) The App Setting name that contains the client secret of the Client. Cannot be used with client_secret.\" })",
        "required": false,
        "default": "null"
      },
      "auth_settings_v2": {
        "type": "object({ auth_enabled = optional(bool, false) runtime_version = optional(string, \"~1\") config_file_path = optional(string) require_authentication = optional(bool) unauthenticated_action = optional(string, \"RedirectToLoginPage\") default_provider = optional(string) excluded_paths = optional(list(string)) require_https = optional(bool, true) http_route_api_prefix = optional(string, \"/.auth\") forward_proxy_convention = optional(string, \"ForwardProxyConventionNoProxy\") forward_proxy_custom_host_header_name = optional(string) forward_proxy_custom_scheme_header_name = optional(string) active_directory_v2 = optional(object({ client_id = string tenant_auth_endpoint = string client_secret_setting_name = optional(string, null) client_secret_certificate_thumbprint = optional(string, null) jwt_allowed_groups = optional(list(string)) jwt_allowed_client_applications = optional(list(string)) www_authentication_disabled = optional(bool, false) allowed_groups = optional(list(string)) allowed_identities = optional(list(string)) allowed_applications = optional(list(string)) login_parameters = optional(map(string)) allowed_audiences = optional(list(string)) }), null) login = optional(object({ logout_endpoint = optional(string) token_store_enabled = optional(bool, false) token_refresh_extension_time = optional(string, 72) token_store_path = optional(string) token_store_sas_setting_name = optional(string) preserve_url_fragments_for_logins = optional(bool, false) allowed_external_redirect_urls = optional(list(string)) cookie_expiration_convention = optional(string, \"FixedTime\") cookie_expiration_time = optional(string, \"08:00:00\") validate_nonce = optional(bool, true) nonce_expiration_time = optional(string, \"00:05:00\") })) })",
        "description": "(Optional) An `auth_settings_v2` block supports the following: auth_enabled = \"(Optional) Should the AuthV2 Settings be enabled. Defaults to false.\" runtime_version = \"(Optional) The Runtime Version of the Authentication and Authorisation feature of this App. Defaults to `~1.`\" config_file_path = \"(Optional) The path to the App Auth settings. Relative Paths are evaluated from the Site Root directory.\" require_authentication = \"(Optional) Should the authentication flow be used for all requests.\" unauthenticated_action = \"(Optional) The action to take for requests made without authentication. Possible values include `RedirectToLoginPage`, `AllowAnonymous`, `Return401`, and `Return403`. Defaults to `RedirectToLoginPage`.\" default_provider = \"(Optional) The Default Authentication Provider to use when the unauthenticated_action is set to `RedirectToLoginPage`. Possible values include: `apple`, `azureactivedirectory`, `facebook`, `github`, `google`, `twitter` and the name of your `custom_oidc_v2` provider. Whilst any value will be accepted by the API for default_provider, it can leave the app in an unusable state if this value does not correspond to the name of a known provider (either built-in value, or custom_oidc name) as it is used to build the auth endpoint URI.\" excluded_paths = \"(Optional) The paths which should be excluded from the unauthenticated_action when it is set to RedirectToLoginPage.\" require_https = \"(Optional) Should HTTPS be required on connections? Defaults to true.\" http_route_api_prefix = \"(Optional) The prefix that should precede all the authentication and authorisation paths. Defaults to `/.auth`.\" forward_proxy_convention = \"(Optional) The convention used to determine the url of the request made. Possible values include `ForwardProxyConventionNoProxy`, `ForwardProxyConventionStandard`, `ForwardProxyConventionCustom`. Defaults to `ForwardProxyConventionNoProxy`.\" forward_proxy_custom_host_header_name = \"(Optional) The name of the custom header containing the host of the request.\" forward_proxy_custom_scheme_header_name = \"(Optional) The name of the custom header containing the scheme of the request.\" active_directory_v2 = (Optional) object({ client_id = \"(Required) The ID of the Client to use to authenticate with Azure Active Directory.\" tenant_auth_endpoint = \"(Required) The OpenID Connect Authentication Endpoint for the Azure Active Directory Tenant e.g. `https://login.microsoftonline.com/v2.0/{tenant-guid}/`.\" client_secret_setting_name = \"(Optional) The App Setting name that contains the client secret of the Client.\" client_secret_certificate_thumbprint = \"(Optional) The thumbprint of the certificate used for signing purposes.\" jwt_allowed_groups = \"(Optional) A list of Allowed Groups in the JWT Claim.\" jwt_allowed_client_applications = \"(Optional) A list of Allowed Client Applications in the JWT Claim.\" www_authentication_disabled = \"(Optional) Should the www-authenticate provider should be omitted from the request. Defaults to `false`.\" allowed_groups = \"(Optional) The list of allowed Group Names for the Default Authorisation Policy.\" allowed_identities = \"(Optional) The list of allowed Identities for the Default Authorisation Policy.\" allowed_applications = \"(Optional) The list of allowed Applications for the Default Authorisation Policy.\" login_parameters = \"(Optional) A map of key-value pairs to send to the Authorisation Endpoint when a user logs in.\" allowed_audiences = \"(Optional) Specifies a list of Allowed audience values to consider when validating JWTs issued by Azure Active Directory.\" }) login = (Optional) object({ logout_endpoint = \"(Optional) The endpoint to which logout requests should be made.\" token_store_enabled = \"(Optional) Should the Token Store configuration Enabled. Defaults to `false`\" token_refresh_extension_time = \"(Optional) The number of hours after session token expiration that a session token can be used to call the token refresh API. Defaults to `72` hours.\" token_store_path = \"(Optional) The directory path in the App Filesystem in which the tokens will be stored.\" token_store_sas_setting_name = \"(Optional) The name of the app setting which contains the SAS URL of the blob storage containing the tokens.\" preserve_url_fragments_for_logins = \"(Optional) Should the fragments from the request be preserved after the login request is made. Defaults to `false`.\" allowed_external_redirect_urls = \"(Optional) External URLs that can be redirected to as part of logging in or logging out of the app. This is an advanced setting typically only needed by Windows Store application backends.\" cookie_expiration_convention = \"(Optional) The method by which cookies expire. Possible values include: `FixedTime`, and `IdentityProviderDerived`. Defaults to `FixedTime`\" cookie_expiration_time = \"(Optional) The time after the request is made when the session cookie should expire. Defaults to `08:00:00`\" validate_nonce = \"(Optional) Should the nonce be validated while completing the login flow. Defaults to `true`\" nonce_expiration_time = \"(Optional) The time after the request is made when the nonce should expire. Defaults to `00:05:00`\" })",
        "required": false,
        "default": "null"
      },
      "backend_storage_key_vault_secret_id": {
        "type": "string",
        "description": "(Optional) The Key Vault Secret ID, optionally including version, that contains the Connection String to connect to the storage account for this Function App.",
        "required": false,
        "default": "null"
      },
      "backend_storage_uses_managed_identity": {
        "type": "string",
        "description": "(Optional) Should the Function App use Managed Identity to access the storage account. Conflicts with `storage_account_access_key`.",
        "required": false,
        "default": "null"
      },
      "backup": {
        "type": "object({ name = string storage_account_url = string enabled = bool schedule = object({ frequency_interval = number frequency_unit = string keep_at_least_one_backup = optional(bool) retention_period_days = optional(number) start_time = optional(string) }) })",
        "description": "(Optional) A map of key-value pairs for App Settings and custom values. object({ name = \"(Required) The name which should be used for this Backup.\" storage_account_url = \"(Required) The SAS URL to the container.\" enabled = \"(Optional) Should this backup job be enabled? Defaults to true.\" schedule = \"(Required) A schedule block as defined below\" frequency_interval = \"(Required) How often the backup should be executed (e.g. for weekly backup, this should be set to 7 and frequency_unit should be set to Day).\" frequency_unit = \"(Required) The unit of time for how often the backup should take place. Possible values include: Day and Hour.\" keep_at_least_one_backup = \"(Optional) Should the service keep at least one backup, regardless of age of backup. Defaults to false.\" retention_period_days = \"(Optional) After how many days backups should be deleted. Defaults to 30.\" start_time = \"(Optional) When the schedule should start working in RFC-3339 format.\" })",
        "required": false,
        "default": "null"
      },
      "builtin_logging_enabled": {
        "type": "bool",
        "description": "(Optional) Should built in logging be enabled. Configures AzureWebJobsDashboard app setting based on the configured storage setting.",
        "required": false,
        "default": "true"
      },
      "client_certificate_enabled": {
        "type": "bool",
        "description": "(Optional) Should the function app use Client Certificates.",
        "required": false,
        "default": "false"
      },
      "client_certificate_exclusion_paths": {
        "type": "string",
        "description": "(Optional) The User Assigned Identity ID used for accessing KeyVault secrets. The identity must be assigned to the application in the identity block.",
        "required": false,
        "default": "null"
      },
      "client_certificate_mode": {
        "type": "string",
        "description": "(Optional) The mode of the Function App's client certificates requirement for incoming requests. Possible values are 'Required', 'Optional', and 'OptionalInteractiveUser'.",
        "required": false,
        "default": "null"
      },
      "connection_strings": {
        "type": "list( object({ name = string type = string value = string }) )",
        "description": "(Optional) A list of `connection_strings` objects for the Windows Function App with following arguments: name = \"(Required) The name of the connection string.\" type = \"(Required) Type of database. Possible values include: `APIHub`, `Custom`, `DocDb`, `EventHub`, `MySQL`, `NotificationHub`, `PostgreSQL`, `RedisCache`, `ServiceBus`, `SQLAzure`, and `SQLServer`.\" value = \"(Required) The value of the connection string.\"",
        "required": false,
        "default": "[]"
      },
      "content_share_force_disabled": {
        "type": "bool",
        "description": "(Optional) Should Content Share Settings be disabled. Defaults to `false`.",
        "required": false,
        "default": "false"
      },
      "context": {
        "type": "string",
        "description": "(Optional) Application context information for the resource(s) (max 10 chars).",
        "required": false,
        "default": "null"
      },
      "daily_memory_time_quota": {
        "type": "number",
        "description": "(Optional) The amount of memory in gigabyte-seconds that your app can consume per day, defaults to 0",
        "required": false,
        "default": "0"
      },
      "deployment_slots": {
        "type": "map(object({ name = string service_plan_id = optional(string) app_settings = optional(map(string)) enabled = optional(bool, true) https_only = optional(bool, true) public_network_access_enabled = optional(bool, false) functions_extension_version = optional(string, \"~4\") builtin_logging_enabled = optional(bool, true) client_certificate_enabled = optional(bool) client_certificate_mode = optional(string, \"Optional\") client_certificate_exclusion_paths = optional(string) daily_memory_time_quota = optional(number, 0) ftp_publish_basic_authentication_enabled = optional(bool, false) webdeploy_publish_basic_authentication_enabled = optional(bool, false) key_vault_reference_identity_id = optional(string) virtual_network_subnet_id = optional(string) vnet_image_pull_enabled = optional(bool, false) content_share_force_disabled = optional(bool) virtual_network_backup_restore_enabled = optional(bool, false) connection_strings = optional(list(object({ name = string type = string value = string })), []) identity_ids = optional(list(string)) site_config = optional(object({ always_on = optional(bool, false) api_definition_url = optional(string) api_management_api_id = optional(string) app_command_line = optional(string) app_scale_limit = optional(number) application_insights_connection_string = optional(string) application_insights_key = optional(string) default_documents = optional(list(string)) elastic_instance_minimum = optional(number) ftps_state = optional(string, \"Disabled\") health_check_path = optional(string) health_check_eviction_time_in_min = optional(number) http2_enabled = optional(bool, false) load_balancing_mode = optional(string, \"LeastRequests\") managed_pipeline_mode = optional(string, \"Integrated\") minimum_tls_version = optional(string, \"1.2\") pre_warmed_instance_count = optional(number) remote_debugging_enabled = optional(bool, false) runtime_scale_monitoring_enabled = optional(bool) scm_minimum_tls_version = optional(string, \"1.2\") scm_use_main_ip_restriction = optional(bool, false) ip_restriction_default_action = optional(string, \"Allow\") scm_ip_restriction_default_action = optional(string, \"Allow\") use_32_bit_worker = optional(bool, true) vnet_route_all_enabled = optional(bool, true) websockets_enabled = optional(bool, false) worker_count = optional(number) auto_swap_slot_name = optional(string) application_stack = optional(object({ java_version = optional(string) dotnet_version = optional(string) node_version = optional(string) use_dotnet_isolated_runtime = optional(bool) powershell_core_version = optional(string) use_custom_runtime = optional(bool) })) app_service_logs = optional(object({ disk_quota_mb = optional(number, 35) retention_period_days = optional(number) })) ip_restrictions = optional(list(object({ ip_address = optional(string) service_tag = optional(string) virtual_network_subnet_id = optional(string) name = optional(string) priority = optional(number, 65000) action = optional(string, \"Allow\") description = optional(string) headers = optional(object({ x_azure_fdid = optional(list(string)) x_fd_health_probe = optional(number) x_forwarded_for = optional(list(string)) x_forwarded_host = optional(list(string)) })) })), []) scm_ip_restrictions = optional(list(object({ ip_address = optional(string) service_tag = optional(string) virtual_network_subnet_id = optional(string) name = optional(string) priority = optional(number, 65000) action = optional(string, \"Allow\") description = optional(string) headers = optional(object({ x_azure_fdid = optional(list(string)) x_fd_health_probe = optional(number) x_forwarded_for = optional(list(string)) x_forwarded_host = optional(list(string)) })) })), []) })) auth_settings = optional(object({ enabled = bool additional_login_parameters = optional(map(string)) allowed_external_redirect_urls = optional(list(string)) default_provider = optional(string) issuer = optional(string) runtime_version = optional(string) token_refresh_extension_hours = optional(number, 72) token_store_enabled = optional(bool, false) unauthenticated_client_action = optional(string) active_directory = optional(object({ client_id = string client_secret = optional(string) allowed_audiences = optional(list(string)) client_secret_setting_name = optional(string) })) })) auth_settings_v2 = optional(object({ auth_enabled = optional(bool, false) runtime_version = optional(string, \"~1\") config_file_path = optional(string) require_authentication = optional(bool) unauthenticated_action = optional(string, \"RedirectToLoginPage\") default_provider = optional(string) excluded_paths = optional(list(string)) require_https = optional(bool, true) http_route_api_prefix = optional(string, \"/.auth\") forward_proxy_convention = optional(string, \"NoProxy\") forward_proxy_custom_host_header_name = optional(string) forward_proxy_custom_scheme_header_name = optional(string) active_directory_v2 = optional(object({ client_id = string tenant_auth_endpoint = string client_secret_setting_name = optional(string) client_secret_certificate_thumbprint = optional(string) jwt_allowed_groups = optional(list(string)) jwt_allowed_client_applications = optional(list(string)) www_authentication_disabled = optional(bool, false) allowed_groups = optional(list(string)) allowed_identities = optional(list(string)) allowed_applications = optional(list(string)) login_parameters = optional(map(string)) allowed_audiences = optional(list(string)) })) login = optional(object({ logout_endpoint = optional(string) token_store_enabled = optional(bool, false) token_refresh_extension_time = optional(number, 72) token_store_path = optional(string) token_store_sas_setting_name = optional(string) preserve_url_fragments_for_logins = optional(bool, false) allowed_external_redirect_urls = optional(list(string)) cookie_expiration_convention = optional(string, \"FixedTime\") cookie_expiration_time = optional(string, \"08:00:00\") validate_nonce = optional(bool, true) nonce_expiration_time = optional(string, \"00:05:00\") })) })) backup = optional(object({ name = string enabled = optional(bool, true) storage_account_url = string schedule = optional(object({ frequency_interval = number frequency_unit = string keep_at_least_one_backup = optional(bool, false) retention_period_days = optional(number, 30) start_time = optional(string) })) })) storage_account = optional(list(object({ access_key = string account_name = string name = string share_name = string type = string mount_path = optional(string) }))) }))",
        "description": "(Optional) A map of deployment slots to create for the Windows Function App. map(object({ name = \"(Required) Specifies the name of the Function App Slot.\" service_plan_id = \"(Optional) The ID of the Service Plan in which to run this slot. If not specified the same Service Plan as the Windows Function App will be used.\" app_settings = \"(Optional) A map of key-value pairs for App Settings and custom values. Most commonly overridden property for slots (e.g., different config for staging vs production).\" enabled = \"(Optional) Is the Windows Function App Slot enabled. Defaults to `true`.\" https_only = \"(Optional) Can the Function App Slot only be accessed via HTTPS. Defaults to `true`.\" public_network_access_enabled = \"(Optional) Should public network access be enabled for the Function App Slot. Defaults to `false`.\" functions_extension_version = \"(Optional) The runtime version associated with the Function App Slot. Defaults to `~4`.\" builtin_logging_enabled = \"(Optional) Should built in logging be enabled. Defaults to `true`.\" client_certificate_enabled = \"(Optional) Should the Function App Slot use Client Certificates.\" client_certificate_mode = \"(Optional) The mode of the Function App Slot's client certificates requirement. Possible values are `Required`, `Optional`, and `OptionalInteractiveUser`. Defaults to `Optional`.\" client_certificate_exclusion_paths = \"(Optional) Paths to exclude when using client certificates, separated by `;`.\" daily_memory_time_quota = \"(Optional) The amount of memory in gigabyte-seconds that your application is allowed to consume per day. Defaults to `0`.\" ftp_publish_basic_authentication_enabled = \"(Optional) Are the default FTP Basic Authentication publishing credentials enabled. Defaults to `false`.\" webdeploy_publish_basic_authentication_enabled = \"(Optional) Should the default WebDeploy Basic Authentication publishing credentials enabled. Defaults to `false`.\" key_vault_reference_identity_id = \"(Optional) The User Assigned Identity ID used for accessing KeyVault secrets.\" virtual_network_subnet_id = \"(Optional) The subnet id which will be used by this Function App Slot for regional virtual network integration.\" vnet_image_pull_enabled = \"(Optional) Should the Function App Slot use VNet image pull. Defaults to `false`.\" content_share_force_disabled = \"(Optional) Should Content Share be disabled for this Function App Slot.\" virtual_network_backup_restore_enabled = \"(Optional) Should virtual network backup and restore be enabled for this Function App Slot. Defaults to `false`.\" connection_strings = optional(list(object({ name = \"(Required) The name of the Connection String.\" type = \"(Required) The type of the Connection String. Possible values are `APIHub`, `Custom`, `DocDb`, `EventHub`, `MySQL`, `NotificationHub`, `PostgreSQL`, `RedisCache`, `ServiceBus`, `SQLAzure`, and `SQLServer`.\" value = \"(Required) The connection string value.\" }))) identity_ids = \"(Optional) A list of User Assigned Managed Identity IDs to be assigned to this Windows Function App Slot. If not specified, inherits from parent Function App.\" site_config = (Optional) object({ always_on = \"(Optional) Should the Function App Slot be always on. Defaults to `false`.\" api_definition_url = \"(Optional) The URL to the API Definition for this Function App Slot.\" api_management_api_id = \"(Optional) The ID of the API Management API for this Function App Slot.\" app_command_line = \"(Optional) The App command line to launch.\" app_scale_limit = \"(Optional) The number of workers this Function App Slot can scale out to. Only applicable to apps on the `Consumption` and `Premium` plan.\" application_insights_connection_string = \"(Optional) The Connection String for linking the Function App Slot to Application Insights.\" application_insights_key = \"(Optional) The Instrumentation Key for connecting the Function App Slot to Application Insights.\" default_documents = \"(Optional) Specifies a list of Default Documents for the Windows Function App Slot.\" elastic_instance_minimum = \"(Optional) The minimum number of elastic instances for this Function App Slot. Only affects apps on Elastic Premium plans.\" ftps_state = \"(Optional) The State of FTP / FTPS service. Possible values are `FtpsOnly` and `Disabled`. Defaults to `Disabled`.\" health_check_path = \"(Optional) The path to the Health Check endpoint.\" health_check_eviction_time_in_min = \"(Optional) The amount of time in minutes that a node can be unhealthy before being removed from the load balancer. Possible values are between `2` and `10`. Only valid in conjunction with `health_check_path`.\" http2_enabled = \"(Optional) Should HTTP2 be enabled. Defaults to `false`.\" load_balancing_mode = \"(Optional) The Site load balancing mode. Possible values include `WeightedRoundRobin`, `LeastRequests`, `LeastResponseTime`, `WeightedTotalTraffic`, `RequestHash`, and `PerSiteRoundRobin`. Defaults to `LeastRequests`.\" managed_pipeline_mode = \"(Optional) The Managed Pipeline Mode. Possible values are `Integrated` and `Classic`. Defaults to `Integrated`.\" minimum_tls_version = \"(Optional) The minimum TLS version. Possible values are `1.0`, `1.1`, and `1.2`. Defaults to `1.2`.\" pre_warmed_instance_count = \"(Optional) The number of pre-warmed instances for this Function App Slot. Only affects apps on Elastic Premium plans.\" remote_debugging_enabled = \"(Optional) Should Remote Debugging be enabled. Defaults to `false`.\" runtime_scale_monitoring_enabled = \"(Optional) Should Scale Monitoring of the Functions Runtime be enabled.\" scm_minimum_tls_version = \"(Optional) The minimum TLS version for SCM. Possible values are `1.0`, `1.1`, and `1.2`. Defaults to `1.2`.\" scm_use_main_ip_restriction = \"(Optional) Should the SCM site use the same IP restrictions as the main site. Defaults to `false`.\" ip_restriction_default_action = \"(Optional) The Default action for traffic that does not match any ip_restriction rule. Possible values include `Allow` and `Deny`. Defaults to `Allow`.\" scm_ip_restriction_default_action = \"(Optional) The Default action for traffic that does not match any scm_ip_restriction rule. Possible values include `Allow` and `Deny`. Defaults to `Allow`.\" use_32_bit_worker = \"(Optional) Should the Function App Slot run in 32 bit mode. Defaults to `true`.\" vnet_route_all_enabled = \"(Optional) Should all traffic be routed to the VNet. Defaults to `false`.\" websockets_enabled = \"(Optional) Should Web Sockets be enabled. Defaults to `false`.\" worker_count = \"(Optional) The number of Workers for this Function App Slot.\" auto_swap_slot_name = \"(Optional) The name of the slot to automatically swap to when deployment to this slot completes successfully.\" application_stack = (Optional) object({ java_version = \"(Optional) The version of Java to use. Supported versions include `1.8`, `11`, `17`, and `21` (In-Preview).\" dotnet_version = \"(Optional) The version of .NET to use. Possible values include `3.1`, `6.0`, `7.0`, `8.0` and `9.0`.\" node_version = \"(Optional) The version of Node to use. Possible values include `~12`, `~14`, `~16`, `~18`, `~20`, `~22`, and `~24`.\" use_dotnet_isolated_runtime = \"(Optional) Should the DotNet process use an isolated runtime.\" powershell_core_version = \"(Optional) The version of PowerShell Core to use. Possible values include `7`, `7.2`, and `7.4`.\" use_custom_runtime = \"(Optional) Should the Windows Function App Slot use a custom runtime.\" }) app_service_logs = (Optional) object({ disk_quota_mb = \"(Optional) The amount of disk space to use for logs in MB. Valid values are between `25` and `100`. Defaults to `35`.\" retention_period_days = \"(Optional) The retention period for logs in days. Valid values are between `0` and `99999`.\" }) ip_restrictions = (Optional) list(object({ ip_address = \"(Optional) The CIDR notation of the IP or IP Range to match. For example: `10.0.0.0/24` or `192.168.10.1/32`. One and only one of ip_address, service_tag or virtual_network_subnet_id must be specified.\" service_tag = \"(Optional) The Service Tag used for this IP Restriction.\" virtual_network_subnet_id = \"(Optional) The Virtual Network Subnet ID used for this IP Restriction.\" name = \"(Optional) The name which should be used for this ip_restriction\" priority = \"(Optional) The priority value of this ip_restriction. Defaults to `65000`.\" action = \"(Optional) The action to take when the rule is matched. Possible values include: `Allow` and `Deny`. Defaults to `Allow`.\" description = \"(Optional) The Description of this IP Restriction.\" headers = (Optional) object({ x_azure_fdid = \"(Optional) A list of Azure Front Door IDs.\" x_fd_health_probe = \"(Optional) Specifies if a Front Door Health Probe should be expected. The only possible value is `1`.\" x_forwarded_for = \"(Optional) A list of addresses for which matching should be applied. Omitting this value means allow any.\" x_forwarded_host = \"(Optional) A list of Hosts for which matching should be applied.\" }) })) scm_ip_restrictions = (Optional) list(object({ ip_address = \"(Optional) The CIDR notation of the IP or IP Range to match. For example: `10.0.0.0/24` or `192.168.10.1/32`. One and only one of ip_address, service_tag or virtual_network_subnet_id must be specified.\" service_tag = \"(Optional) The Service Tag used for this IP Restriction.\" virtual_network_subnet_id = \"(Optional) The Virtual Network Subnet ID used for this IP Restriction.\" name = \"(Optional) The name which should be used for this scm_ip_restriction\" priority = \"(Optional) The priority value of this scm_ip_restriction. Defaults to `65000`.\" action = \"(Optional) The action to take when the rule is matched. Possible values include: `Allow` and `Deny`. Defaults to `Allow`.\" description = \"(Optional) The Description of this IP Restriction.\" headers = (Optional) object({ x_azure_fdid = \"(Optional) A list of Azure Front Door IDs.\" x_fd_health_probe = \"(Optional) Specifies if a Front Door Health Probe should be expected. The only possible value is `1`.\" x_forwarded_for = \"(Optional) A list of addresses for which matching should be applied. Omitting this value means allow any.\" x_forwarded_host = \"(Optional) A list of Hosts for which matching should be applied.\" }) })) }) auth_settings = (Optional) object({ enabled = \"(Required) Should the Authentication / Authorization feature be enabled for the Windows Function App Slot.\" additional_login_parameters = \"(Optional) Specifies a map of login Parameters to send to the OpenID Connect authorization endpoint when a user logs in.\" allowed_external_redirect_urls = \"(Optional) Specifies a list of External URLs that can be redirected to as part of logging in or logging out.\" default_provider = \"(Optional) The default authentication provider to use when multiple providers are configured. Possible values include: `AzureActiveDirectory`, `Facebook`, `Google`, `MicrosoftAccount`, `Twitter`, `Github`.\" issuer = \"(Optional) The OpenID Connect Issuer URI that represents the entity which issues access tokens.\" runtime_version = \"(Optional) The Runtime Version of the Authentication / Authorization feature in use.\" token_refresh_extension_hours = \"(Optional) The number of hours after session token expiration that a session token can be used to call the token refresh API. Defaults to `72` hours.\" token_store_enabled = \"(Optional) Should the Windows Function App Slot durably store platform-specific security tokens that are obtained during login flows. Defaults to `false`.\" unauthenticated_client_action = \"(Optional) The action to take when an unauthenticated client attempts to access the app. Possible values include: `RedirectToLoginPage`, `AllowAnonymous`.\" active_directory = (Optional) object({ client_id = \"(Required) The ID of the Client to use to authenticate with Azure Active Directory.\" client_secret = \"(Optional) The Client Secret for the Client ID. Cannot be used with client_secret_setting_name.\" allowed_audiences = \"(Optional) Specifies a list of Allowed audience values to consider when validating JWTs issued by Azure Active Directory.\" client_secret_setting_name = \"(Optional) The App Setting name that contains the client secret of the Client. Cannot be used with client_secret.\" }) }) auth_settings_v2 = (Optional) object({ auth_enabled = \"(Optional) Should the AuthV2 Settings be enabled. Defaults to `false`.\" runtime_version = \"(Optional) The Runtime Version of the Authentication and Authorization feature. Defaults to `~1`.\" config_file_path = \"(Optional) The path to the App Auth settings.\" require_authentication = \"(Optional) Should the authentication flow be used for all requests.\" unauthenticated_action = \"(Optional) The action to take for requests made without authentication. Possible values include `RedirectToLoginPage`, `AllowAnonymous`, `Return401`, and `Return403`. Defaults to `RedirectToLoginPage`.\" default_provider = \"(Optional) The Default Authentication Provider to use when the unauthenticated_action is set to `RedirectToLoginPage`.\" excluded_paths = \"(Optional) The paths which should be excluded from the unauthenticated_action when it is set to RedirectToLoginPage.\" require_https = \"(Optional) Should HTTPS be required on connections. Defaults to `true`.\" http_route_api_prefix = \"(Optional) The prefix that should precede all the authentication and authorisation paths. Defaults to `/.auth`.\" forward_proxy_convention = \"(Optional) The convention used to determine the url of the request made. Possible values include `NoProxy`, `Standard`, `Custom`. Defaults to `NoProxy`.\" forward_proxy_custom_host_header_name = \"(Optional) The name of the custom header containing the host of the request.\" forward_proxy_custom_scheme_header_name = \"(Optional) The name of the custom header containing the scheme of the request.\" active_directory_v2 = (Optional) object({ client_id = \"(Required) The ID of the Client to use to authenticate with Azure Active Directory.\" tenant_auth_endpoint = \"(Required) The Azure Tenant Endpoint for the Authenticating Tenant. e.g. `https://login.microsoftonline.com/{tenant-guid}/v2.0/`\" client_secret_setting_name = \"(Optional) The App Setting name that contains the client secret of the Client. A setting with this name must exist in `app_settings` to function correctly.\" client_secret_certificate_thumbprint = \"(Optional) The thumbprint of the certificate used for signing purposes.\" jwt_allowed_groups = \"(Optional) A list of Allowed Groups in the JWT Claim.\" jwt_allowed_client_applications = \"(Optional) A list of Allowed Client Applications in the JWT Claim.\" www_authentication_disabled = \"(Optional) Should the www-authenticate provider should be omitted from the request. Defaults to `false`.\" allowed_groups = \"(Optional) The list of allowed Group Names for the Default Authorisation Policy.\" allowed_identities = \"(Optional) The list of allowed Identities for the Default Authorisation Policy.\" allowed_applications = \"(Optional) The list of allowed Applications for the Default Authorisation Policy.\" login_parameters = \"(Optional) A map of key-value pairs to send to the Authorisation Endpoint when a user logs in.\" allowed_audiences = \"(Optional) Specifies a list of Allowed audience values to consider when validating JWTs issued by Azure Active Directory.\" }) login = (Optional) object({ logout_endpoint = \"(Optional) The endpoint to which logout requests should be made.\" token_store_enabled = \"(Optional) Should the Token Store configuration Enabled. Defaults to `false`.\" token_refresh_extension_time = \"(Optional) The number of hours after session token expiration that a session token can be used to call the token refresh API. Defaults to `72` hours.\" token_store_path = \"(Optional) The directory path in the App Filesystem in which the tokens will be stored.\" token_store_sas_setting_name = \"(Optional) The name of the app setting which contains the SAS URL of the blob storage containing the tokens.\" preserve_url_fragments_for_logins = \"(Optional) Should the fragments from the request be preserved after the login request is made. Defaults to `false`.\" allowed_external_redirect_urls = \"(Optional) External URLs that can be redirected to as part of logging in or logging out of the app.\" cookie_expiration_convention = \"(Optional) The method by which cookies expire. Possible values include: `FixedTime`, and `IdentityProviderDerived`. Defaults to `FixedTime`.\" cookie_expiration_time = \"(Optional) The time after the request is made when the session cookie should expire. Defaults to `08:00:00`.\" validate_nonce = \"(Optional) Should the nonce be validated while completing the login flow. Defaults to `true`.\" nonce_expiration_time = \"(Optional) The time after the request is made when the nonce should expire. Defaults to `00:05:00`.\" }) }) backup = (Optional) object({ name = \"(Required) The name for this Backup.\" enabled = \"(Optional) Should this backup be enabled. Defaults to `true`.\" storage_account_url = \"(Required) The SAS URL to the container.\" schedule = (Optional) object({ frequency_interval = \"(Required) How often the backup should be executed.\" frequency_unit = \"(Required) The unit of time for how often the backup should take place. Possible values are `Day` or `Hour`.\" keep_at_least_one_backup = \"(Optional) Should the service keep at least one backup, regardless of age of backup. Defaults to `false`.\" retention_period_days = \"(Optional) After how many days backups should be deleted. Defaults to `30`.\" start_time = \"(Optional) When the schedule should start working in RFC-3339 format.\" }) }) storage_account = (Optional) list(object({ access_key = \"(Required) The Access key for the storage account.\" account_name = \"(Required) The Name of the Storage Account.\" name = \"(Required) The name of this Storage Account.\" share_name = \"(Required) The Name of the File Share or Container Name for Blob storage.\" type = \"(Required) The Azure Storage Type. Possible values are `AzureFiles`.\" mount_path = \"(Optional) The path at which to mount the storage share.\" })) }))",
        "required": false,
        "default": "{}"
      },
      "enable_systemassigned_identity": {
        "type": "bool",
        "description": "(Optional) Whether to enable SystemAssigned Identity, by default pattern deploys UserAssigned Idenity.",
        "required": false,
        "default": "true"
      },
      "enabled": {
        "type": "bool",
        "description": "(Required) Is the function app enabled? Default is true",
        "required": false,
        "default": "true"
      },
      "environment": {
        "type": "string",
        "description": "(Required) The environment where the resource is deployed. Given as abbreviation to leave more characters to the other naming components (max 3 chars).",
        "required": true
      },
      "firewall_private_ip_address": {
        "type": "string",
        "description": "(Optional). Azure firewall private Ip Address. Used when creating routes to firewall during route table creation.",
        "required": false,
        "default": "null"
      },
      "functions_extension_version": {
        "type": "string",
        "description": "(Optional) The runtime version associated with the Function App. Defaults to ~4.",
        "required": false,
        "default": "\"~4\""
      },
      "instance": {
        "type": "string",
        "description": "(Optional) Instance number, if context includes/requires multiple resources of the same type (max 3 int).",
        "required": false,
        "default": "null"
      },
      "key_vault_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) deploy_kv_and_pe = optional(bool, true) key_vault_id = optional(string, null) enabled_for_deployment = optional(bool, false) enabled_for_disk_encryption = optional(bool, true) enabled_for_template_deployment = optional(bool, false) soft_delete_retention_days = optional(number, 30) sku_name = optional(string, \"premium\") public_network_access_enabled = optional(bool, false) kv_admin_role_app_spn_object_id = optional(string, null) # kv_secret_expiration_in_months = number # kv_secret_instance = optional(string, null) network_acls = optional(object({ bypass = optional(string, \"None\") default_action = optional(string, \"Deny\") ip_rules = optional(list(string), []) virtual_network_subnet_ids = optional(list(string), []) }), {}) private_endpoint = object({ is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_id = optional(string, null) private_connection_resource_alias = optional(string, null) request_message = optional(string, null) kv_ip_configuration = optional(map(object({ private_ip_address = string subresource_name = optional(string, \"vault\") member_name = optional(string, \"default\") })), {}) }) })",
        "description": "Object with configuration for the Keyvault. As defined below context = context to be used for KeyVaults being deployed resources. instance = (Optional) Instance to be used for KeyVaults being deployed resources. deploy_kv_and_pe = (Optional) Boolean to deploy KeyVault and Private Endpoint for KeyVault. Default is true. key_vault_id = (Optional) KeyVault ID to be used for Private Endpoint for KeyVault. Required if deploy_kv_and_pe is false. enabled_for_deployment = (Optional) Boolean to enable KeyVault for deployment. Default is false. enabled_for_disk_encryption = (Optional) Boolean to enable KeyVault for disk encryption. Default is true. enabled_for_template_deployment = (Optional) Boolean to enable KeyVault for template deployment. Default is false. soft_delete_retention_days = (Optional) The number of days that items should be retained for once soft-deleted. Default is 30. sku_name = (Optional) Sku name for KeyVault. Default is premium. public_network_access_enabled = (Optional) Specifies whether public network access is enabled for the Key Vault. Default is false. kv_admin_role_app_spn_object_id = (Optional) The object id of the Service Principal for Key Vault Admin role assignment. network_acls = (Optional) Object with network ACLs for KeyVault. As defined below { bypass = (Optional) Specifies whether traffic is bypassed for Azure services. Default is None. Possible values are None, AzureServices, Logging, Metrics, or AzureServices,Logging,Metrics. default_action = (Optional) Specifies the default action of allow or deny when no other rules match. Default is Deny. Possible values are Allow and Deny. ip_rules = (Optional) List of IP addresses or CIDR ranges to whitelist. Only IPV4 addresses are allowed. This list must not include IP address ranges within Azure services. This list can include IP addresses ranges from on-premises ranges. virtual_network_subnet_ids = (Optional) List of virtual network subnet ids to whitelist. This list must not include IP address ranges within Azure services. } private_endpoint = (Optional) Object with private endpoint configuration for KeyVault. As defined below { is_manual_connection = (Optional) Boolean to indicate if the connection is manual. Default is false. static_ip_required = (Optional) Boolean to indicate if static IP is required. Default is false. private_connection_resource_id = (Optional) Resource ID of the private connection. Required if is_manual_connection is true. private_connection_resource_alias = (Optional) Resource alias of the private connection. Required if is_manual_connection is true. request_message = (Optional) A message passed to the owner of the remote resource when the private endpoint attempts to establish the connection. kv_ip_configuration = (Optional) Map of IP configuration objects for KeyVault. As defined below { private_ip_address = (Required) Private IP address of the IP configuration. subresource_name = (Optional) Subresource name of the IP configuration. Default is vault. member_name = (Optional) Member name of the IP configuration. Default is default. } }",
        "required": true
      },
      "key_vault_tags": {
        "type": "map(any)",
        "description": "(Optional) Tags to be set on each resource.",
        "required": false,
        "default": "{}"
      },
      "location": {
        "type": "string",
        "description": "(Required) Location of the resource group.",
        "required": true
      },
      "loganalytics_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) deploy_loganalytics = optional(bool, true) log_analytics_resource_id = optional(string, null) sku = optional(string, \"PerGB2018\") retention_in_days = optional(number, 365) allow_resource_only_permissions = optional(bool, true) daily_quota_gb = optional(number, -1) internet_ingestion_enabled = optional(bool, false) internet_query_enabled = optional(bool, false) reservation_capacity_in_gb_per_day = optional(number, null) cmk_for_query_forced = optional(bool, false) data_classification_tag_value = optional(string, \"\") identity = optional(object({ type = string identity_ids = optional(set(string)) }), { type = \"SystemAssigned\" identity_ids = null }) data_collection_rule_id = optional(string, null) log_analytics_data_export = optional(object({ name = string table_names = list(string) destination_resource_id = string enabled = optional(bool) }), null) log_analytics_custom_tables = optional(map(object({ description = optional(string, null) restoredLogs = optional(object({ endRestoreTime = string sourceTable = string startRestoreTime = string }), null) columns = list(object({ name = string displayName = string type = string description = optional(string, null) dataTypeHint = optional(string, null) })) searchResults = optional(object({ description = string endSearchTime = string limit = number query = string startSearchTime = string }), null) retention_in_days = optional(number, 365) total_retention_days = optional(number, 365) plan = string })), {}) })",
        "description": "Object with cofigration for Log anlytics Workspace , As Defined below context = \"(Optional) The context to be used for Log analytics. If not provided the pattern conterxt is picked.\" instance = \"(Optional) The instance to be used for Log analytics. If not provided the pattern conterxt is picked.\" deploy_loganalytics = \"(Optional) Weather to deploy log analytics. Defaults to True.\" log_analytics_resource_id = \"(Optional) The ARM resource id for log analytics worksapce to be used for app isnghts deployment.\" sku = \"(Optional) The SKU for the Log Analytics workspace.\" retention_in_days = \"(Optional) The retention in days for the Log Analytics workspace.\" allow_resource_only_permissions = \"(Optional) The resource permissions log for the Log Analytics workspace.\" daily_quota_gb = \"(Optional) The daily quota in GB for the Log Analytics workspace.\" internet_ingestion_enabled = \"(Optional) The internet ingestion enabled for the Log Analytics workspace.\" internet_query_enabled = \"(Optional) The internet query enabled for the Log Analytics workspace.\" reservation_capacity_in_gb_per_day = \"(Optional) The reservation capacity in GB per day for the Log Analytics workspace.\" cmk_for_query_forced = \"(Optional) The cmk_for_query_forced for the Log Analytics workspace.\" data_classification_tag_value = \"(Optional) The value of the data classification tag required by policy.\" identity = (Optional) An identity block as defined below object({ type = \"(Required) Specifies the identity type of the Log Analytics Workspace. Possible values are SystemAssigned (where Azure will generate a Service Principal for you) and UserAssigned where you can specify the Service Principal IDs in the identity_ids field\" identity_ids = \"(Optional) Specifies a list of user managed identity ids to be assigned. Required if type is UserAssigned\" }) data_collection_rule_id = \"(Optional) The ID of the Data Collection Rule to use for this workspace.\" log_analytics_data_export = (Optional) object({ name = (Required) The name of the Log Analytics Data Export Rule. Changing this forces a new Log Analytics Data Export Rule to be created. table_names = (Required) A list of table names to export to the destination resource, for example: [\"Heartbeat\", \"SecurityEvent\"]. destination_resource_id = (Required) The destination resource ID. It should be a storage account, an event hub namespace or an event hub. If the destination is an event hub namespace, an event hub would be created for each table automatically. enabled = (Optional) Is this Log Analytics Data Export Rule enabled? Possible values include true or false. Defaults to false. }) log_analytics_custom_tables = (Optional) A map of custom tables to create in the Log Analytics workspace. map(object({ description = \"(Optional) Description of the custom table's purpose and usage.\" restoredLogs = Optional(object({ endRestoreTime = \"(Required) The timestamp to end the restore by (UTC).\" sourceTable = \"(Required) The table to restore data from.\" startRestoreTime = \"(Required) The timestamp to start the restore from (UTC).\" })) columns = (Required)list(object({ name = \"(Required) The name of the column.\" displayName = \"(Required) The display name of the column.\" type = \"(Required) The data type of the column (e.g., 'string', 'datetime', 'int', 'real', 'boolean', 'guid', 'dynamic').\" description = \"(Optional) Description of the column's purpose.\" dataTypeHint = \"(Optional) Column data type logical hint. Valid values: 'armPath', 'guid', 'ip', 'uri'.\" })) searchResults = Optional(object({ description = \"(Required) Description of the search results configuration.\" endSearchTime = \"(Required) The end time for search results in ISO 8601 format.\" limit = \"(Required) The maximum number of search results to return.\" query = \"(Required) The KQL query to execute for search results.\" startSearchTime = \"(Required) The start time for search results in ISO 8601 format.\" })) })) retention_in_days = \"(Optional) The table retention period in days (4-4383, or -1 for default).\" total_retention_days = \"(Optional) The total retention period in days (4-4383, or -1 for default).\" plan = \"(Required) The table plan type. Valid values: 'Analytics', 'Basic'.\" }))",
        "required": false,
        "default": "null"
      },
      "network_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) deploy_windowsfunctionapp_subnet = optional(bool, true) windowsfunctionapp_subnet_id = optional(string, null) windowsfunctionapp_subnet_address_prefix = optional(string, null) special_subnet_name = optional(string, null) service_endpoints = optional(list(string), []) default_outbound_access = optional(bool, false) security_rules = optional(map(object({ name = string description = string priority = number direction = string access = string protocol = string source_port_range = string destination_port_range = string source_address_prefix = string destination_address_prefix = string source_application_security_group_ids = list(string) destination_application_security_group_ids = list(string) })), {}) route_table = optional(object({ route_table_id = optional(string, null) bgp_route_propagation_enabled = optional(bool, true) routes = optional(map(object({ name = string address_prefix = string next_hop_type = string next_hop_in_ip_address = optional(string, null) })), {}) }), { route_table_id = null bgp_route_propagation_enabled = true routes = {} } ) private_endpoint = optional(object({ is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_id = optional(string, null) private_connection_resource_alias = optional(string, null) request_message = optional(string, null) ip_configuration = optional(map(object({ private_ip_address = string subresource_name = optional(string, \"sites\") member_name = optional(string, \"sites\") })), {}) }), {}) })",
        "description": "- `network_config` - (Optional). Object with cofigration for the network config. As defined below - `context` - (Optional) Context to be used for Storage Account being deployed resources. - `instance` - (Optional) instance to be used for Storage Account being deployed resources. - `deploy_windowsfunctionapp_subnet` - (Optional) Boolean to deploy delegated subnet for Function APP. - `windowsfunctionapp_subnet_address_prefix`- (Optional) Address prefix for the delegated subnet where Function APP will be deployed. Value must be passed if `deploy_windowsfunctionapp_subnet` is `true`. - `windowsfunctionapp_subnet_id` - (Optional) Existing delegated subnet id for Function APP. - `special_subnet_name` - (Optional) Used for Azure reserved subnet names. - `default_outbound_access` - (Optional) Boolean flag to enable default outbound access for the subnet. Default is false. - `service_endpoints` - (Optional) The list of Service endpoints to associate with the subnet. Possible values include: Microsoft.AzureActiveDirectory, Microsoft.AzureCosmosDB, Microsoft.ContainerRegistry, Microsoft.EventHub, Microsoft.KeyVault, Microsoft.ServiceBus, Microsoft.Sql, Microsoft.Storage and Microsoft.Web.. - `security_rules` - (Optional). Map of objects mentioned below security rules of the NSG to be deployed and attached to Subnet. As defined below - `name` - (Required) The name of the security rule. - `description` - (Optional) A description for this rule. Restricted to 140 characters. - `priority` - (Required) Specifies the priority of the rule. The value can be between 100 and 4096. The priority number must be unique for each rule in the collection. The lower the priority number, the higher the priority of the rule. - `direction` - (Required) The direction specifies if rule will be evaluated on incoming or outgoing traffic. Possible values are Inbound and Outbound. - `access` - (Required) Specifies whether network traffic is allowed or denied. Possible values are Allow and Deny. - `protocol` - (Required) Network protocol this rule applies to. Possible values include Tcp, Udp, Icmp, Esp, Ah or * (which matches all). - `source_port_range` - (Optional) Source Port or Range. Integer or range between 0 and 65535 or * to match any. This is required if source_port_ranges is not specified. - `destination_port_range` - (Optional) Destination Port or Range. Integer or range between 0 and 65535 or * to match any. This is required if destination_port_ranges is not specified. - `source_address_prefix` - (Optional) CIDR or source IP range or * to match any IP. Tags such as VirtualNetwork, AzureLoadBalancer and Internet can also be used. This is required if source_address_prefixes is not specified. - `destination_address_prefix` - (Optional) CIDR or destination IP range or * to match any IP. Tags such as VirtualNetwork, AzureLoadBalancer and Internet can also be used. This is required if destination_address_prefixes is not specified. - `source_application_security_group_ids` - (Optional) A List of source Application Security Group IDs. - `destination_application_security_group_ids` - (Optional) A List of destination Application Security Group IDs. - `route_table` - (Optional). Object with route table configuration. As defined below - `route_table_id` - (Optional) Route table ID to be used for PSQL Subnet. Required if route is not provided. - `route` - (Optional) A Map of objects mentioned below for route configuration of the route table. - `name` - (Required) The name of the route.\" - `address_prefix` - (Required) The destination CIDR to which the route applies.\" - `next_hop_type` - (Required) The type of Azure hop the packet should be sent to. Possible values are VirtualNetworkGateway, VnetLocal, Internet, VirtualAppliance and None.\" - `next_hop_in_ip_address` - (Optional) The IP address packets should be forwarded to. Next hop values are only allowed in routes where the next hop type is VirtualAppliance.\" - `disable_bgp_route_propagation` - (Optional) Boolean flag which controls propagation of routes learned by BGP on that route table. True means disable. False means enable. Default is false. - `private_endpoints` - (Optional) Configuration for enabling Private Endpoints for the Function APP. - `is_manual_connection` - (Optional) Does the Private Endpoint require Manual Approval from the remote resource owner? Changing this forces a new resource to be created. - `static_ip_required` - (Optional) Whether a Static IP is required to be assigned to Private Endpoint or not.\" - `request_message` - (Optional) A message passed to the owner of the remote resource when the private endpoint attempts to establish the connection to the remote resource. - `ip_configuration` - (Optional) An ip_configuration block as defined below -`private_ip_address` - (Required) Specifies the static IP address within the private endpoint's subnet to be used. Changing this forces a new resource to be created. -`subresource_name` - (Required) Specifies the subresource this IP address applies to. -`member_name` - (Optional) Specifies the member name this IP address applies to.\"",
        "required": false,
        "default": "null"
      },
      "org_id": {
        "type": "string",
        "description": "(Required) Three letter code representing organization/tenant/CSP.",
        "required": true
      },
      "privateendpoint_subnet_id": {
        "type": "string",
        "description": "(Required.) The resource id of the subnet to deploy private endpoints into.",
        "required": false,
        "default": "null"
      },
      "resource_group_name": {
        "type": "string",
        "description": "(Required). The name of the Application Resource Group. This Resource group usually contains the Application resources(app infra resources).",
        "required": true
      },
      "shared_nrtbl_vnet_id": {
        "type": "string",
        "description": "(Reqired). The ARM Resource Id of the Non-Routeable Virtual Network in shared Resource group.",
        "required": true
      },
      "site_config": {
        "type": "object({ enable_appinsights = optional(bool, true) always_on = optional(bool, false) api_definition_url = optional(string) api_management_api_id = optional(string) app_command_line = optional(string) app_scale_limit = optional(number) application_insights_key = optional(string) default_documents = optional(list(string)) elastic_instance_minimum = optional(number) ftps_state = optional(string, \"Disabled\") health_check_path = optional(string) health_check_eviction_time_in_min = optional(number) load_balancing_mode = optional(string, \"LeastRequests\") managed_pipeline_mode = optional(string, \"Integrated\") pre_warmed_instance_count = optional(number) runtime_scale_monitoring_enabled = optional(bool) scm_use_main_ip_restriction = optional(bool, false) ip_restriction_default_action = optional(string, \"Allow\") scm_ip_restriction_default_action = optional(string, \"Allow\") use_32_bit_worker = optional(bool, true) websockets_enabled = optional(bool, false) worker_count = optional(number) application_stack = optional(object({ dotnet_version = optional(string) use_dotnet_isolated_runtime = optional(bool) java_version = optional(string) node_version = optional(string) powershell_core_version = optional(string) use_custom_runtime = optional(bool) })) app_service_logs = optional(object({ disk_quota_mb = optional(number, 35) retention_period_days = optional(number) })) ip_restrictions = optional(list( object({ headers = optional(object({ x_azure_fdid = optional(list(string)) x_fd_health_probe = optional(number) x_forwarded_for = optional(list(string)) x_forwarded_host = optional(list(string)) })) ip_address = optional(string) action = optional(string) name = optional(string) priority = optional(number, 65000) service_tag = optional(string) virtual_network_subnet_id = optional(string) description = optional(string) }) ), []) scm_ip_restrictions = optional(list( object({ headers = optional(object({ x_azure_fdid = optional(list(string)) x_fd_health_probe = optional(number) x_forwarded_for = optional(list(string)) x_forwarded_host = optional(list(string)) })) ip_address = optional(string) action = optional(string) name = optional(string) priority = optional(number, 65000) service_tag = optional(string) virtual_network_subnet_id = optional(string) description = optional(string) }) ), []) })",
        "description": "(Required) The `site_config` block supports the following: enable_appinsights = \"(optional) Whether to configure App Insights. Defaults to True. always_on = \"(Optional) If this Windows Function App has Always On enabled. Defaults to `false`.\" api_definition_url = \"(Optional) The URL of the API definition that describes this Windows Function App.\" api_management_api_id = \"(Optional) The ID of the API Management API to use for this Windows Function App.\" app_command_line = \"(Optional) The App command line to launch.\" app_scale_limit = \"(Optional) The number of workers this function app can scale out to. Only applicable to apps on the `Consumption` and `Premium` plan.\" application_insights_key = \"(Optional) The Instrumentation Key for connecting the Windows Function App to Application Insights.\" application_stack = \"(Optional) object({ dotnet_version = \"(Optional) The version of .NET to use. Possible values include `v3.0`, `v4.0`, `v6.0` and `v7.0`.\" use_dotnet_isolated_runtime = \"(Optional) Should the DotNet process use an isolated runtime.\" java_version = \"(Optional) The Version of Java to use. Supported versions include `1.8`, `11` & `17` (In-Preview).\" node_version = \"(Optional) The version of Node to run. Possible values include `~12`, `~14`, `~16` and `~18`.\" powershell_core_version = \"(Optional) The version of PowerShell Core to run. Possible values are `7`, and `7.2`.\" use_custom_runtime = \"(Optional) Should the Windows Function App use a custom runtime?\" }))\" app_service_logs = \"(Optional) object({ disk_quota_mb = \"(Optional) The amount of disk space to use for logs. Valid values are between `25` and `100`. Defaults to `35`.\" retention_period_days = \"(Optional) The retention period for logs in days. Valid values are between `0` and `99999`.(never delete).\" }))\" default_documents = \"(Optional) A list of Default Documents for the Windows Function App.\" elastic_instance_minimum = \"(Optional) The number of minimum instances for this Windows Function App. Only affects apps on Elastic Premium plans.\" ftps_state = \"(Optional) State of FTP / FTPS service for this Windows Function App. Possible values include: `FtpsOnly` and `Disabled`. Defaults to `Disabled`.\" health_check_path = \"(Optional) The path to be checked for this Windows Function App health.\" health_check_eviction_time_in_min = \"(Optional) The amount of time in minutes that a node can be unhealthy before being removed from the load balancer. Possible values are between `2` and `10`. Only valid in conjunction with health_check_path.\" ip_restrictions = \"(Optional) A list of `ip_restriction` objects for the Windows Function App. ip_restriction = \"object({ headers = \"(Optional) object({ x_azure_fdid = \"(Optional) (Optional) Specifies a list of Azure Front Door IDs.\" x_fd_health_probe = \"(Optional) (Optional) Specifies if a Front Door Health Probe should be expected. The only possible value is 1.\" x_forwarded_for = \"(Optional) Specifies a list of addresses for which matching should be applied. Omitting this value means allow any.\" x_forwarded_host = \"(Optional) Specifies a list of Hosts for which matching should be applied.\" }))\" ip_address = \"(Optional) The CIDR notation of the IP or IP Range to match. For example: `10.0.0.0/24` or `192.168.10.1/32`\" action = \"(Optional) The action to take when the rule is matched. Possible values include: `Allow` and `Deny`.\" name = \"(Optional) The name which should be used for this ip_restriction\" priority = \"(Optional) The priority value of this ip_restriction. Defaults to `65000`.\" service_tag = \"(Optional) The Service Tag used for this IP Restriction.\" virtual_network_subnet_id = \"(Optional) The Virtual Network Subnet ID used for this IP Restriction.\" description = \"(Optional) The Description of this IP Restriction.\" }))\" load_balancing_mode = \"(Optional) The Site load balancing mode. Possible values include: `WeightedRoundRobin`, `LeastRequests`, `LeastResponseTime`, `WeightedTotalTraffic`, `RequestHash`, `PerSiteRoundRobin`. Defaults to `LeastRequests` if omitted.\" managed_pipeline_mode = \"(Optional) The Managed Pipeline Mode for this Windows Function App. Possible values include: `Integrated` and `Classic`. Defaults to `Integrated`.\" pre_warmed_instance_count = \"(Optional) The number of pre-warmed instances for this Windows Function App. Only affects apps on Elastic Premium plans.\" runtime_scale_monitoring_enabled = \"(Optional) Should runtime scale monitoring be enabled for this Windows Function App?\" scm_ip_restrictions = \"(Optional) A list of `scm_ip_restriction` objects for the Windows Function App. scm_ip_restriction = \"object({ headers = \"(Optional) object({ x_azure_fdid = \"(Optional) (Optional) Specifies a list of Azure Front Door IDs.\" x_fd_health_probe = \"(Optional) (Optional) Specifies if a Front Door Health Probe should be expected. The only possible value is 1.\" x_forwarded_for = \"(Optional) Specifies a list of addresses for which matching should be applied. Omitting this value means allow any.\" x_forwarded_host = \"(Optional) Specifies a list of Hosts for which matching should be applied.\" }))\" ip_address = \"(Optional) The CIDR notation of the IP or IP Range to match. For example: `10.0.0.0/24` or `192.168.10.1/32`\" action = \"(Optional) The action to take when the rule is matched. Possible values include: `Allow` and `Deny`.\" name = \"(Optional) The name which should be used for this ip_restriction\" priority = \"(Optional) The priority value of this ip_restriction. Defaults to `65000`.\" service_tag = \"(Optional) The Service Tag used for this IP Restriction.\" virtual_network_subnet_id = \"(Optional) The Virtual Network Subnet ID used for this IP Restriction.\" description = \"(Optional) The Description of this IP Restriction.\" }))\" scm_use_main_ip_restriction = \"(Optional) Should the Windows Function App ip_restriction configuration be used for the SCM also. Default is `false`\" ip_restriction_default_action = \"(Optional) The Default action for traffic that does not match any ip_restriction rule. Possible values include `Allow` and `Deny`. Defaults to `Allow`.\" scm_ip_restriction_default_action = \"(Optional) The Default action for traffic that does not match any scm_ip_restriction rule. Possible values include `Allow` and `Deny`. Defaults to `Allow`.\" use_32_bit_worker = \"(Optional) Should the Windows Function App use a 32-bit worker process. Defaults to `true`.\" websockets_enabled = \"(Optional) Should Web Sockets be enabled for this Windows Function App? Defaults to `false`.\" worker_count = \"(Optional) The number of Workers for this Windows Function App.\"",
        "required": false,
        "default": "{}"
      },
      "slot_traffic_routing": {
        "type": "list(object({ slot_name = string traffic_percentage = number }))",
        "description": "(Optional) List of slot traffic routing rules. slot_name = \"(Required) The name of the slot to route traffic to. Must match an existing slot name.\" traffic_percentage = \"(Required) The percentage of traffic to route to this slot. Value must be between 0 and 100. Total of all slots should not exceed 100.\"",
        "required": false,
        "default": "[]"
      },
      "sticky_settings": {
        "type": "object({ app_setting_names = optional(list(string)) connection_string_names = optional(list(string)) })",
        "description": "(Optional) An Sticky settings block as defined below object({ app_setting_names = \"(Optional) A list of app_setting names that the Windows Function App will not swap between Slots when a swap operation is triggered.\" connection_string_names = \"(Optional) A list of connection_string names that the Windows Function App will not swap between Slots when a swap operation is triggered.\" })",
        "required": false,
        "default": "null"
      },
      "storage_account_config": {
        "type": "object({ context = optional(string, null) instance = optional(string, null) create_role_assignment = optional(bool, true) cmk_key_name = optional(string, null) https_traffic_only_enabled = optional(bool, true) cross_tenant_replication_enabled = optional(bool, false) deploy_sa_and_pe = optional(bool, true) storage_account_id = optional(string, null) primary_access_key = optional(string, null) primary_blob_endpoint = optional(string, null) windowsfunctionapp_storage_share_name = optional(string, null) enable_file_share_AADDS_authentication = optional(bool, true) account_tier = optional(string, \"Standard\") account_replication_type = optional(string, \"GRS\") account_kind = optional(string, \"StorageV2\") access_tier = optional(string, \"Hot\") large_file_share_enabled = optional(bool, false) nfsv3_enabled = optional(bool, false) enable_key_access = optional(bool, true) retention_policy_days = optional(number, 30) kv_secret_expiration_date = optional(string, null) storage_policy_required = optional(bool, false) cmk_key_expiration_date = optional(string, null) default_to_oauth_authentication = optional(bool, false) network_access_enabled = optional(bool, false) restore_policy = optional(number, 29) static_website = optional(any, null) custom_domain = optional(map(any), {}) key_vault_tags = optional(map(any), {}) rules = optional(map(object({ name = string enabled = bool filters = object({ blob_types = set(string) prefix_match = set(string) match_blob_index_tag = optional(object({ name = string operation = optional(string, \"==\") value = string })) }) actions = object({ base_blob = optional(object({ tier_to_cool_after_days_since_modification_greater_than = optional(number) tier_to_cool_after_days_since_last_access_time_greater_than = optional(number) tier_to_cool_after_days_since_creation_greater_than = optional(number) auto_tier_to_hot_from_cool_enabled = optional(bool, false) tier_to_archive_after_days_since_modification_greater_than = optional(number) tier_to_archive_after_days_since_last_access_time_greater_than = optional(number) tier_to_archive_after_days_since_creation_greater_than = optional(number) tier_to_archive_after_days_since_last_tier_change_greater_than = optional(number) tier_to_cold_after_days_since_modification_greater_than = optional(number) tier_to_cold_after_days_since_last_access_time_greater_than = optional(number) tier_to_cold_after_days_since_creation_greater_than = optional(number) delete_after_days_since_modification_greater_than = optional(number) delete_after_days_since_last_access_time_greater_than = optional(number) delete_after_days_since_creation_greater_than = optional(number) })) snapshot = optional(object({ change_tier_to_archive_after_days_since_creation = optional(number) tier_to_archive_after_days_since_last_tier_change_greater_than = optional(number) change_tier_to_cool_after_days_since_creation = optional(number) tier_to_cold_after_days_since_creation_greater_than = optional(number) delete_after_days_since_creation_greater_than = optional(number) })) version = optional(object({ change_tier_to_archive_after_days_since_creation = optional(number) tier_to_archive_after_days_since_last_tier_change_greater_than = optional(number) change_tier_to_cool_after_days_since_creation = optional(number) tier_to_cold_after_days_since_creation_greater_than = optional(number) delete_after_days_since_creation = optional(number) })) }) })), {} ) cmk_key_rotation_policy = optional(object({ notify_before_expiry = optional(string, \"P358D\") time_before_expiry = optional(string, \"P7D\") time_after_creation = optional(string, null) expire_after = optional(string, \"P365D\") }), {}) encryption_scopes = optional(map(object({ name = string key_vault_key_id = string })), {}) network_rules = optional(object({ default_action = string bypass = list(string) ip_rules = list(string) virtual_network_subnet_ids = list(string) }), { default_action = \"Deny\" ip_rules = [] virtual_network_subnet_ids = [] bypass = [\"AzureServices\"] } ) private_endpoints = optional(object({ blob = optional(object({ enable = optional(bool, false) is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_alias = optional(string, null) request_message = optional(string, null) ip_configuration = optional(map(object({ private_ip_address = string subresource_name = string member_name = optional(string, \"default\") })), null) }), ( { enable = false is_manual_connection = false static_ip_required = false private_connection_resource_alias = null request_message = null } ) ) file_share = optional(object({ enable = optional(bool, false) is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_alias = optional(string, null) request_message = optional(string, null) ip_configuration = optional(map(object({ private_ip_address = string subresource_name = string member_name = optional(string, \"default\") })), null) }), ( { enable = false is_manual_connection = false static_ip_required = false private_connection_resource_alias = null request_message = null } ) ) queue = optional(object({ enable = optional(bool, false) is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_alias = optional(string, null) request_message = optional(string, null) ip_configuration = optional(map(object({ private_ip_address = string subresource_name = string member_name = optional(string, \"default\") })), null) }), ( { enable = false is_manual_connection = false static_ip_required = false private_connection_resource_alias = null request_message = null } ) ) table = optional(object({ enable = optional(bool, false) is_manual_connection = optional(bool, false) static_ip_required = optional(bool, false) private_connection_resource_alias = optional(string, null) request_message = optional(string, null) ip_configuration = optional(map(object({ private_ip_address = string subresource_name = string member_name = optional(string, \"default\") })), null) }), ( { enable = false is_manual_connection = false static_ip_required = false private_connection_resource_alias = null request_message = null } ) ) }), { blob = { enable = false is_manual_connection = false static_ip_required = false } file_share = { enable = false is_manual_connection = false static_ip_required = false } queue = { enable = false is_manual_connection = false static_ip_required = false } table = { enable = false is_manual_connection = false static_ip_required = false } } ) file_shares = optional(map(object({ metadata = optional(map(any), null) context = optional(string, null) instance = string quota = string enabled_protocol = optional(string, \"SMB\") access_tier = optional(string, \"TransactionOptimized\") })), {}) queues = optional(map(object({ metadata = optional(map(any), null) context = optional(string, null) instance = string })), {}) containers = optional(map(object({ metadata = optional(map(any), null) context = optional(string, null) instance = string })), {}) tables = optional(map(object({ context = optional(string, null) instance = string })), {}) })",
        "description": "storage_account=(Optional) Map of Inputs required for the deployment of Storage Account for the Application Pattern. context = \"(Required). Context to be used for all networking resources.\" instance = \"(Required). Instance to be used for all networking resources.\" deploy_sa_and_pe = \"(Optional). Whether to deploy Storage account or use exisiting.\" storage_account_id = \"(Optional). If not deploying a Storge account, this needs to be passed. ARM resource id of the storage account\" windowsfunctionapp_storage_share_name = \"(Optional). If not deploying a Storge account, this needs to be passed. file share name to be used as content share in windows azure functions.\" primary_access_key = \"(Optional). If not deploying a Storge account, this needs to be passed. primary access key of the storage account.\" primary_blob_endpoint = \"(Optional). If not deploying a Storge account, this needs to be passed. primary blob endpoint for storge account. \" account_tier = \"(Optional) Defines the Tier to use for this storage account. Valid options are Standard and Premium.\" account_replication_type = \"(Optional) Defines the type of replication to use for this storage account. Valid options are LRS, GRS, RAGRS, ZRS, GZRS and RAGZRS.\" account_kind = \"(Optional) Defines the Kind of account. Valid options are BlobStorage, BlockBlobStorage, FileStorage, Storage and StorageV2.\" access_tier = \"(Optional) Defines the Access Tier for the Storage Account.Possible values are: `Cool`, `Hot`.\" large_file_share_enabled = \"(Optional) Set to `true`, the Storage Account will be enabled for large file shares.\" nfsv3_enabled = \"(Optional) Set to `true`, the `NFSV3` protocol will be enabled.\" https_traffic_only_enabled = \"(Optional) Is HTTPS traffic only to be allowed for the Storage Account.\" cross_tenant_replication_enabled = \"(Optional) Is Cross Tenant Replication to be enabled for the Storage Account.\" enable_key_access = \"(Optional) If set to `false` any requests to the account that are authorized with Shared Key, including shared access signatures (SAS), will be denied.\" retention_policy_days = \"(Optional) Specifies the number of days that the containers, blob, queues and share should be retained, the value must be between between 1 and 365.\" cmk_key_expiration_date = \"(Required) Expiration UTC datetime (Y-m-d'T'H:M:S'Z') for customer managed encryption key.\" kv_secret_expiration_date = \"(Required) Expiration UTC datetime (Y-m-d'T'H:M:S'Z') for Storage Account access key Key vault Secret.\" enable_file_share_AADDS_authentication = \"(Optional) Enable authentication to File Shares via Active Directory Domain Services`.\" storage_policy_required = \"(Optional) Boolean to indicate if Storage Account should have a Storage Account Policy.\" default_to_oauth_authentication = \"(Optional) Whether to default to OAuth authentication for this storage account.\" network_access_enabled = \"(Optional) Whether public network access is enabled for this storage account.\" restore_policy = \"(Optional) The number of days for point in time restore. Must be less than the days specified in delete_retention_policy time_before_expiry = \"(Optional) Rotate automatically at a duration before expiry as an ISO 8601 duration.\" time_after_creation = \"(Optional) Rotate automatically at a duration before expiry as an ISO 8601 duration.\" expire_after = \"(Optional) Expire a Key Vault Key after given duration as an ISO 8601 duration.\" }) encryption_scopes = \"(Optional) Map of the encryption scopes in the Storage Account.\" key_vault_id= \"(Required) ID of the existing Key vault to store the Customer Managed Key for Encryption.\" network_rules = (Optional) A network_rules block as defined below object({ default_action = \"(Required) Specifies the default action of allow or deny when no other rules match. Possible values are Allow and Deny.\" bypass = \"(Required) Specifies whether traffic is bypassed for Azure services. Default is None. Possible values are None, AzureServices, Logging, Metrics, or AzureServices,Logging,Metrics.\" ip_rules = \"(Required) List of IP addresses or CIDR ranges to whitelist. Only IPV4 addresses are allowed. This list must not include IP address ranges within Azure services. This list can include IP addresses ranges from on-premises ranges.\" virtual_network_subnet_ids = \"(Required) List of virtual network subnet ids to whitelist. This list must not include IP address ranges within Azure services.\" }) private_endpoints = \"Configuration for enabling Private Endpoints for the Storage Account.\" key_vault= object({ enable = optional(bool, true) is_manual_connection = \"(Optional) Does the Private Endpoint require Manual Approval from the remote resource owner? Changing this forces a new resource to be created.\" static_ip_required = \"(Optional) Whether a Static IP is required to be assigned to Private Endpoint or not.\" private_connection_resource_alias = \"(Optional) The Service Alias of the Private Link Enabled Remote Resource which this Private Endpoint should be connected to.\" request_message = \"(Optional) A message passed to the owner of the remote resource when the private endpoint attempts to establish the connection to the remote resource.\" ip_configuration = (Optional) An ip_configuration block as defined below map(object({ private_ip_address = \"(Required) Specifies the static IP address within the private endpoint's subnet to be used. Changing this forces a new resource to be created.\" subresource_name = \"(Required) Specifies the subresource this IP address applies to.\" member_name = \"(Optional) Specifies the member name this IP address applies to.\" })) }) blob = object({ enable = optional(bool, true) is_manual_connection = \"(Optional) Does the Private Endpoint require Manual Approval from the remote resource owner? Changing this forces a new resource to be created.\" static_ip_required = \"(Optional) Whether a Static IP is required to be assigned to Private Endpoint or not.\" private_connection_resource_alias = \"(Optional) The Service Alias of the Private Link Enabled Remote Resource which this Private Endpoint should be connected to.\" request_message = \"(Optional) A message passed to the owner of the remote resource when the private endpoint attempts to establish the connection to the remote resource.\" ip_configuration = (Optional) An ip_configuration block as defined below map(object({ private_ip_address = \"(Required) Specifies the static IP address within the private endpoint's subnet to be used. Changing this forces a new resource to be created.\" subresource_name = \"(Required) Specifies the subresource this IP address applies to.\" member_name = \"(Optional) Specifies the member name this IP address applies to.\" })) }) file_share = object({ enable = optional(bool, true) is_manual_connection = \"(Optional) Does the Private Endpoint require Manual Approval from the remote resource owner? Changing this forces a new resource to be created.\" static_ip_required = \"(Optional) Whether a Static IP is required to be assigned to Private Endpoint or not.\" private_connection_resource_alias = \"(Optional) The Service Alias of the Private Link Enabled Remote Resource which this Private Endpoint should be connected to.\" request_message = \"(Optional) A message passed to the owner of the remote resource when the private endpoint attempts to establish the connection to the remote resource.\" ip_configuration = (Optional) An ip_configuration block as defined below map(object({ private_ip_address = \"(Required) Specifies the static IP address within the private endpoint's subnet to be used. Changing this forces a new resource to be created.\" subresource_name = \"(Required) Specifies the subresource this IP address applies to.\" member_name = \"(Optional) Specifies the member name this IP address applies to.\" })) }) queue = object({ enable = optional(bool, true) is_manual_connection = \"(Optional) Does the Private Endpoint require Manual Approval from the remote resource owner? Changing this forces a new resource to be created.\" static_ip_required = \"(Optional) Whether a Static IP is required to be assigned to Private Endpoint or not.\" private_connection_resource_alias = \"(Optional) The Service Alias of the Private Link Enabled Remote Resource which this Private Endpoint should be connected to.\" request_message = \"(Optional) A message passed to the owner of the remote resource when the private endpoint attempts to establish the connection to the remote resource.\" ip_configuration = (Optional) An ip_configuration block as defined below map(object({ private_ip_address = \"(Required) Specifies the static IP address within the private endpoint's subnet to be used. Changing this forces a new resource to be created.\" subresource_name = \"(Required) Specifies the subresource this IP address applies to.\" member_name = \"(Optional) Specifies the member name this IP address applies to.\" })) }) table = object({ enable = optional(bool, true) is_manual_connection = \"(Optional) Does the Private Endpoint require Manual Approval from the remote resource owner? Changing this forces a new resource to be created.\" static_ip_required = \"(Optional) Whether a Static IP is required to be assigned to Private Endpoint or not.\" private_connection_resource_alias = \"(Optional) The Service Alias of the Private Link Enabled Remote Resource which this Private Endpoint should be connected to.\" request_message = \"(Optional) A message passed to the owner of the remote resource when the private endpoint attempts to establish the connection to the remote resource.\" ip_configuration = (Optional) An ip_configuration block as defined below map(object({ private_ip_address = \"(Required) Specifies the static IP address within the private endpoint's subnet to be used. Changing this forces a new resource to be created.\" subresource_name = \"(Required) Specifies the subresource this IP address applies to.\" member_name = \"(Optional) Specifies the member name this IP address applies to.\" })) }) subnet_id = optional(string, null) }), null) file_shares = optional(map(object({ metadata = \"(Optional) A mapping of MetaData for the File Share.\" instance = \"(Required) Instance number, if context includes/requires multiple resources of the same type.\" quota = \"(Required) The maximum size of the share, in gigabytes. For Standard storage accounts, this must be 1GB (or higher) and at most 5120 GB (5 TB). For Premium FileStorage storage accounts, this must be greater than 100 GB and at most 102400 GB (100 TB).\" enabled_protocol = \"(Optional) The protocol used for the share. Possible values are SMB and NFS.\" access_tier = \"(Optional) The access tier of the File Share. Possible values are Hot, Cool and TransactionOptimized, Premium.\" })), {}) queues = optional(map(object({ metadata = \"(Optional) A mapping of MetaData for the File Share.\" instance = \"(Required) Instance number, if context includes/requires multiple resources of the same type.\" })), {}) containers = optional(map(object({ metadata = \"(Optional) A mapping of MetaData for the File Share.\" instance = \"(Required) Instance number, if context includes/requires multiple resources of the same type.\" })), {}) tables = optional(map(object({ instance = \"(Required) Instance number, if context includes/requires multiple resources of the same type.\" })), {}) rules = (Optional) A map of rule block as documented below: object({ name = \"(Required) The name of the rule. Rule name is case-sensitive. It must be unique within a policy.\" enabled = \"(Required) Boolean to specify whether the rule is enabled.\" filters = \"(Required) The filters block supports the following: object({ blob_types = \"(Required) An array of predefined values. Valid options are `blockBlob` and `appendBlob`.\" prefix_match = \"(Optional) An array of strings for prefixes to be matched.\" match_blob_index_tag = \"(Optional) A match_blob_index_tag block as defined below. The block defines the blob index tag based filtering for blob objects.\" object({ name = \"(Required) The filter tag name used for tag based filtering for blob objects.\" operation = \"(Optional) The comparison operator which is used for object comparison and filtering. Possible value is ==. Defaults to ==.\" value = \"(Required) The filter tag value used for tag based filtering for blob objects.\" }) })\" actions = \"(Required) An actions block as documented below: object({ base_blob = (Optional) A base_blob block as documented below: object({ tier_to_cool_after_days_since_modification_greater_than = \"(Optional) The age in days after last modification to tier blobs to cool storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" tier_to_cool_after_days_since_last_access_time_greater_than = \"(Optional) The age in days after last access time to tier blobs to cool storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" tier_to_cool_after_days_since_creation_greater_than = \"(Optional) The age in days after creation to cool storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" auto_tier_to_hot_from_cool_enabled = \"(Optional) Whether a blob should automatically be tiered from cool back to hot if it's accessed again after being tiered to cool. Defaults to false.\" tier_to_archive_after_days_since_modification_greater_than = \"(Optional) The age in days after last modification to tier blobs to archive storage. Supports blob currently at Hot or Cool tier. Must be between 0 and 99999.\" tier_to_archive_after_days_since_last_access_time_greater_than = \"(Optional) The age in days after last access time to tier blobs to archive storage. Supports blob currently at Hot or Cool tier. Must be between 0 and99999.\" tier_to_archive_after_days_since_creation_greater_than = \"(Optional) The age in days after creation to archive storage. Supports blob currently at Hot or Cool tier. Must be between 0 and99999.\" tier_to_archive_after_days_since_last_tier_change_greater_than = \"(Optional) The age in days after last tier change to the blobs to skip to be archved. Must be between 0 and 99999.\" tier_to_cold_after_days_since_modification_greater_than = \"(Optional) The age in days after last modification to tier blobs to cold storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" tier_to_cold_after_days_since_last_access_time_greater_than = \"(Optional) The age in days after last access time to tier blobs to cold storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" tier_to_cold_after_days_since_creation_greater_than = \"(Optional) The age in days after creation to cold storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" delete_after_days_since_modification_greater_than = \"(Optional) The age in days after last modification to delete the blob. Must be between 0 and 99999.\" delete_after_days_since_last_access_time_greater_than = \"(Optional) The age in days after last access time to delete the blob. Must be between 0 and 99999.\" delete_after_days_since_creation_greater_than = \"(Optional) The age in days after creation to delete the blob. Must be between 0 and 99999.\" }) snapshot = (Optional) A snapshot block as documented below: object({ change_tier_to_archive_after_days_since_creation = \"(Optional) The age in days after creation to tier blob snapshot to archive storage. Must be between 0 and 99999.\" tier_to_archive_after_days_since_last_tier_change_greater_than = \"(Optional) The age in days after last tier change to the blobs to skip to be archved. Must be between 0 and 99999.\" change_tier_to_cool_after_days_since_creation = \"(Optional) The age in days after creation to tier blob snapshot to cool storage. Must be between 0 and 99999.\" tier_to_cold_after_days_since_creation_greater_than = \"(Optional) The age in days after creation to cold storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" delete_after_days_since_creation_greater_than = \"(Optional) The age in days after creation to delete the blob snapshot. Must be between 0 and 99999.\" }) version = (Optional) A version block as documented below: object({ change_tier_to_archive_after_days_since_creation = \"(Optional) The age in days after creation to tier blob version to archive storage. Must be between 0 and 99999.\" tier_to_archive_after_days_since_last_tier_change_greater_than = \"(Optional) The age in days after last tier change to the blobs to skip to be archved. Must be between 0 and 99999.\" change_tier_to_cool_after_days_since_creation = \"(Optional) The age in days creation create to tier blob version to cool storage. Must be between 0 and 99999.\" tier_to_cold_after_days_since_creation_greater_than = \"(Optional) The age in days after creation to cold storage. Supports blob currently at Hot tier. Must be between 0 and 99999.\" delete_after_days_since_creation = \"(Optional) The age in days after creation to delete the blob version. Must be between 0 and 99999.\" }) })\" }) `",
        "required": false,
        "default": "null"
      },
      "storage_accounts": {
        "type": "list(object({ access_key = string account_name = string name = string share_name = string type = string mount_path = optional(string) }))",
        "description": "(Optional) An storage_account block supports the following: object({ access_key = \"(Required) The Access key for the storage account.\" account_name = \"(Required) The Name of the Storage Account.\" name = \"(Required) The name which should be used for this Storage Account.\" share_name = \"(Required) The Name of the File Share or Container Name for Blob storage.\" type = \"(Required) The Azure Storage Type. Possible values include AzureFiles and AzureBlob.\" mount_path = \"(Optional) The path at which to mount the storage share.\" })",
        "required": false,
        "default": "[]"
      },
      "tags": {
        "type": "map(any)",
        "description": "(Optional) Tags to be set on each resource.",
        "required": false,
        "default": "{}"
      },
      "vnet_image_pull_enabled": {
        "type": "bool",
        "description": "(Optional) Specifies whether traffic for the image pull should be routed over virtual network. Defaults to false.",
        "required": false,
        "default": "false"
      },
      "zip_deploy_file": {
        "type": "string",
        "description": "(Optional) The local path and filename of the Zip packaged application to deploy to this Windows Function App.Using this value requires `WEBSITE_RUN_FROM_PACKAGE=1` to be set on the App in app_settings. Refer to the Azure docs for further details.",
        "required": false,
        "default": "null"
      }
    }
  },
  "outputs": {
    "application_insights": {
      "description": "The application_insights module outputs."
    },
    "appserviceplan": {
      "description": "The list appserviceplan module outputs."
    },
    "file_shares": {
      "description": "The file_shares module outputs."
    },
    "keyvault": {
      "description": "The keyvault module outputs."
    },
    "loganalytics": {
      "description": "The loganalytics module outputs."
    },
    "networksecuritygroup_windowsfunctionapp_subnet": {
      "description": "The networksecuritygroup_windowsfunctionapp_subnet module outputs."
    },
    "privateendpoint-akv": {
      "description": "The privateendpoint-akv module outputs."
    },
    "privateendpoint_blob": {
      "description": "The privateendpoint-blob module outputs."
    },
    "privateendpoint_fileshare": {
      "description": "The privateendpoint-fileshare module outputs."
    },
    "privateendpoint_queue": {
      "description": "The privateendpoint-queue module outputs."
    },
    "privateendpoint_table": {
      "description": "The privateendpoint-table module outputs."
    },
    "routetable_windowsfunctionapp_subnet": {
      "description": "The routetable_windowsfunctionapp_subnet module outputs."
    },
    "storageaccount": {
      "description": "The storage account module outputs."
    },
    "storagecontainer": {
      "description": "The storagecontainer module outputs."
    },
    "storagequeue": {
      "description": "The storagequeue module outputs."
    },
    "storagetable": {
      "description": "The storagetable module outputs."
    },
    "userassignedidentity_appinsights_uai_metrics_publisher": {
      "description": "The userassignedidentity_appinsights_uai_metrics_publisher module outputs."
    },
    "userassignedidentity_storage": {
      "description": "The userassignedidentity_storage module outputs."
    },
    "userassignedidentity_windowsfunctionapp": {
      "description": "The userassignedidentity_windowsfunctionapp module outputs."
    },
    "windowsfunctionapp": {
      "description": "The windowsfunctionapp module output."
    },
    "windowsfunctionapp_subnet": {
      "description": "The windowsfunctionapp_subnet module outputs."
    }
  },
  "agent_instructions": {
    "summary": "To provision 'Windowsfunctionapp' via Terraform, call this CPF module with the inputs defined below. Required inputs have no default and must be provided.",
    "module_type_notes": "This is a service pattern module. It bundles multiple related services/configurations.",
    "naming_convention": "Resource names are auto-generated by the module using app_id, environment, and region.",
    "prerequisite_modules": []
  }
}